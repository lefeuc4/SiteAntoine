---
phase: quick
plan: 260405-7jm
type: execute
wave: 1
depends_on: []
files_modified:
  - src/payload.config.ts
  - src/scripts/seed-mentions-legales.ts
  - package.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Admin opening Mentions Legales global sees pre-filled content in Lexical editor"
    - "Public /mentions-legales page renders the seeded content identically to the old fallback"
    - "Headings render properly in both admin editor and public page"
  artifacts:
    - path: "src/scripts/seed-mentions-legales.ts"
      provides: "Standalone seed script for mentions legales content"
    - path: "src/payload.config.ts"
      provides: "HeadingFeature added to Lexical editor"
  key_links:
    - from: "src/scripts/seed-mentions-legales.ts"
      to: "mentions-legales global"
      via: "payload.updateGlobal({ slug: 'mentions-legales' })"
      pattern: "updateGlobal.*mentions-legales"
---

<objective>
Pre-fill the Mentions Legales global's richText field with the fallback content currently hardcoded in the public page JSX, so the admin editor shows it pre-populated.

Purpose: Antoine sees the legal text in the Payload admin editor and can edit it directly, instead of seeing an empty editor while the public page shows hardcoded fallback.
Output: A seed script that populates the global, and HeadingFeature enabled in the editor config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/payload.config.ts
@src/globals/MentionsLegales.ts
@src/app/(frontend)/mentions-legales/page.tsx
@src/scripts/seed.ts (for pattern reference — lexicalParagraph helper, Payload import style)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HeadingFeature to Lexical editor config</name>
  <files>src/payload.config.ts</files>
  <action>
    Import HeadingFeature from '@payloadcms/richtext-lexical' alongside existing imports.
    Add HeadingFeature() to the features array in the lexicalEditor() call.
    This is needed because the mentions legales content uses h2 headings, but the current editor config only has Bold, Italic, Lists, Link, FixedToolbar — no heading support.
    Place HeadingFeature() as the first entry in the features array (before BoldFeature) for logical ordering.
  </action>
  <verify>
    <automated>cd /home/lefeuc4/claude/SiteAntoine && grep -q "HeadingFeature" src/payload.config.ts && echo "OK"</automated>
  </verify>
  <done>HeadingFeature is imported and included in the Lexical editor features array</done>
</task>

<task type="auto">
  <name>Task 2: Create seed script for Mentions Legales content</name>
  <files>src/scripts/seed-mentions-legales.ts, package.json</files>
  <action>
    Create src/scripts/seed-mentions-legales.ts — a standalone script that:
    1. Imports getPayload and config (same pattern as src/scripts/seed.ts)
    2. Builds a Lexical JSON document matching the fallback content from mentions-legales/page.tsx (lines 44-111)
    3. Calls payload.updateGlobal({ slug: 'mentions-legales', data: { contenu: lexicalDoc } })
    4. Logs success and calls process.exit(0)

    The Lexical JSON structure must include:
    - A root node with type 'root', direction 'ltr', format '', indent 0, version 1
    - Children array with heading and paragraph nodes

    For each section in the fallback content, create nodes as follows:

    **"Editeur du site" section:**
    - heading node: type 'heading', tag 'h2', text "Editeur du site"
    - paragraph: "Nom : [A COMPLETER]"
    - paragraph: "SIRET : [A COMPLETER]"
    - paragraph: "Adresse : [A COMPLETER]"
    - paragraph: "Email : [A COMPLETER]"

    **"Hebergement" section:**
    - heading node: type 'heading', tag 'h2', text "Hebergement"
    - paragraph: "Ce site est heberge par Vercel Inc., 440 N Barranca Ave #4133, Covina, CA 91723, USA."

    **"Directeur de publication" section:**
    - heading node: type 'heading', tag 'h2', text "Directeur de publication"
    - paragraph: "Antoine Profit"

    **"Donnees personnelles et RGPD" section:**
    - heading node: type 'heading', tag 'h2', text "Donnees personnelles et RGPD"
    - paragraph with mixed content: "Conformement au Reglement General sur la Protection des Donnees (RGPD) et a la loi Informatique et Libertes, vous disposez d'un droit d'acces, de rectification, d'effacement et de portabilite de vos donnees personnelles. Pour exercer ces droits, contactez-nous via la " + link node { url: '/contact', text: 'page contact' } + "."

    **"Cookies et traceurs" section:**
    - heading node: type 'heading', tag 'h2', text "Cookies et traceurs"
    - paragraph: "Ce site utilise Vercel Analytics pour mesurer son audience de maniere anonyme. Cet outil ne depose pas de cookies de tracage et ne collecte pas d'informations personnelles identifiables. Aucune donnee n'est revendue a des tiers."

    **"Photos et consentement clients" section:**
    - heading node: type 'heading', tag 'h2', text "Photos et consentement clients"
    - paragraph with link: "Les photos avant/apres publiees sur ce site sont diffusees avec le consentement explicite des personnes concernees, conformement aux recommandations de la CNIL. Toute demande de retrait peut etre adressee via la " + link { url: '/contact', text: 'page contact' } + "."

    **"Contact" section:**
    - heading node: type 'heading', tag 'h2', text "Contact"
    - paragraph with link: "Pour toute question relative a ce site ou a vos donnees personnelles, rendez-vous sur la " + link { url: '/contact', text: 'page contact' } + "."

    Lexical node formats reference:
    - Heading node: { type: 'heading', tag: 'h2', children: [textNode], direction: 'ltr', format: '', indent: 0, version: 1 }
    - Paragraph node: { type: 'paragraph', children: [textNode...], direction: 'ltr', format: '', indent: 0, textFormat: 0, version: 1 }
    - Text node: { type: 'text', text: '...', format: 0, detail: 0, mode: 'normal', style: '', version: 1 }
    - Link node: { type: 'link', children: [textNode], direction: 'ltr', format: '', indent: 0, version: 3, fields: { url: '/contact', newTab: false, linkType: 'custom' } }

    Also add a package.json script: "seed:mentions" -> "tsx src/scripts/seed-mentions-legales.ts"
  </action>
  <verify>
    <automated>cd /home/lefeuc4/claude/SiteAntoine && npx tsx src/scripts/seed-mentions-legales.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Running pnpm seed:mentions populates the mentions-legales global with the full legal content. The admin editor shows the pre-filled text with headings. The public page renders the seeded content instead of the hardcoded fallback.</done>
</task>

</tasks>

<verification>
1. Run `pnpm seed:mentions` — script completes without errors
2. Open Payload admin at /admin/globals/mentions-legales — editor shows all 7 sections with h2 headings
3. Visit /mentions-legales public page — content renders from DB (same visual result as old fallback)
</verification>

<success_criteria>
- Mentions Legales global has pre-filled content visible in Payload admin
- Public page renders DB content (hasContent check passes) instead of fallback JSX
- All 7 sections present with proper headings and link nodes pointing to /contact
- Seed script is idempotent (can run multiple times safely — updateGlobal overwrites)
</success_criteria>

<output>
After completion, create `.planning/quick/260405-7jm-pre-remplir-editeur-admin-mentions-legal/260405-7jm-SUMMARY.md`
</output>
