---
phase: quick
plan: 260405-8ah
type: execute
wave: 1
depends_on: []
files_modified:
  - src/globals/Accueil.ts
  - src/payload.config.ts
  - src/components/sections/HeroSection.tsx
  - src/components/sections/CTABandeau.tsx
  - src/app/(frontend)/page.tsx
  - src/scripts/seed-accueil.ts
  - package.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Les champs heroTitre, heroDescription, heroImage, presentation, ctaTitre sont editables dans Payload admin sous Accueil"
    - "La page d'accueil affiche les textes provenant du global Accueil au lieu du texte hardcode"
    - "Le seed pre-remplit les valeurs par defaut dans le global Accueil"
  artifacts:
    - path: "src/globals/Accueil.ts"
      provides: "Global config avec 5 champs"
    - path: "src/scripts/seed-accueil.ts"
      provides: "Seed script pour pre-remplir le global"
  key_links:
    - from: "src/app/(frontend)/page.tsx"
      to: "payload global accueil"
      via: "payload.findGlobal({ slug: 'accueil' })"
    - from: "src/components/sections/HeroSection.tsx"
      to: "src/app/(frontend)/page.tsx"
      via: "props heroTitre, heroDescription, heroImageUrl"
    - from: "src/components/sections/CTABandeau.tsx"
      to: "src/app/(frontend)/page.tsx"
      via: "prop ctaTitre"
---

<objective>
Creer un Global "Accueil" dans Payload admin avec 5 champs editables et brancher les composants de la page d'accueil sur ces donnees au lieu du texte hardcode.

Purpose: Antoine peut modifier les textes et l'image hero de la page d'accueil directement depuis l'admin.
Output: Global Accueil fonctionnel, composants branches, seed script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/globals/MentionsLegales.ts (pattern for Global config)
@src/payload.config.ts (register global)
@src/components/sections/HeroSection.tsx (add props)
@src/components/sections/CTABandeau.tsx (add props)
@src/app/(frontend)/page.tsx (fetch global + pass props)
@src/scripts/seed-mentions-legales.ts (pattern for seed script)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Accueil global and register it</name>
  <files>src/globals/Accueil.ts, src/payload.config.ts</files>
  <action>
Create src/globals/Accueil.ts following the MentionsLegales.ts pattern:
- slug: 'accueil'
- label: 'Accueil'
- admin.description: 'Contenu de la page d\'accueil (hero, presentation, CTA)'
- admin.group: 'Pages' (not 'Configuration' — this is page content, not settings)
- access.read: () => true
- 5 fields:
  1. heroTitre: type 'text', label 'Titre Hero', required: true
  2. heroDescription: type 'textarea', label 'Description Hero', required: true
  3. heroImage: type 'upload', relationTo: 'media', label 'Image Hero'
  4. presentation: type 'textarea', label 'Texte de presentation', required: true
  5. ctaTitre: type 'text', label 'Titre du bandeau CTA', required: true

In src/payload.config.ts:
- Add import: import { Accueil } from './globals/Accueil'
- Add to globals array: globals: [ContactSettings, MentionsLegales, Accueil]

After saving, run: pnpm dev (briefly, to trigger Payload type generation and confirm no errors). Then stop dev server.
  </action>
  <verify>
    <automated>cd /home/lefeuc4/claude/SiteAntoine && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Global Accueil registered, TypeScript compiles, 5 fields defined</done>
</task>

<task type="auto">
  <name>Task 2: Wire components to global data and create seed</name>
  <files>src/components/sections/HeroSection.tsx, src/components/sections/CTABandeau.tsx, src/app/(frontend)/page.tsx, src/scripts/seed-accueil.ts, package.json</files>
  <action>
**HeroSection.tsx** — Add props interface and use props instead of hardcoded text:
```
interface HeroSectionProps {
  heroTitre?: string
  heroDescription?: string
  heroImageUrl?: string
}
export default function HeroSection({ heroTitre, heroDescription, heroImageUrl }: HeroSectionProps)
```
- h1: render heroTitre, fallback to current hardcoded text
- p: render heroDescription, fallback to current hardcoded text
- Image area: if heroImageUrl is provided, render an img tag (or next/image with fill) with the URL. Otherwise keep the placeholder div with "Photo coaching a venir". Use next/image with fill={true} and className="object-cover" for proper responsive behavior.

**CTABandeau.tsx** — Add props:
```
interface CTABandeauProps {
  ctaTitre?: string
}
export default function CTABandeau({ ctaTitre }: CTABandeauProps)
```
- h2: render ctaTitre, fallback to current hardcoded "Pret a transformer ta vie ?"

**page.tsx** — Fetch global and pass props:
- After existing `const payload = await getPayload({ config })`, add:
  ```
  const accueil = await payload.findGlobal({ slug: 'accueil', depth: 1 })
  ```
  depth: 1 to populate the heroImage media relation.
- Pass to HeroSection:
  ```
  <HeroSection
    heroTitre={accueil.heroTitre}
    heroDescription={accueil.heroDescription}
    heroImageUrl={(accueil.heroImage as { url?: string } | undefined)?.url}
  />
  ```
- Replace hardcoded presentation text with accueil.presentation (keep the Link to /mon-histoire after):
  ```
  <p className="...">{accueil.presentation}{' '}<Link ...>Decouvrir mon parcours →</Link></p>
  ```
- Pass to CTABandeau:
  ```
  <CTABandeau ctaTitre={accueil.ctaTitre} />
  ```

**seed-accueil.ts** — Follow seed-mentions-legales.ts pattern:
- Import getPayload and config
- Call payload.updateGlobal with slug 'accueil' and data:
  - heroTitre: "Transforme ton corps. Transforme ta vie."
  - heroDescription: "Coach bien-etre specialise en transformation physique et mentale. Un accompagnement personnalise pour atteindre tes objectifs."
  - presentation: "Coach passionne depuis plus de 10 ans, j'accompagne des femmes et des hommes dans leur transformation physique et mentale. Mon approche allie entrainement personnalise, nutrition adaptee et suivi bienveillant."
  - ctaTitre: "Pret a transformer ta vie ?"
  - Do NOT set heroImage (no media to reference in seed)
- console.log success, process.exit(0), catch block with process.exit(1)

**package.json** — Add script:
  "seed:accueil": "node --env-file=.env.local --import tsx/esm src/scripts/seed-accueil.ts"
  </action>
  <verify>
    <automated>cd /home/lefeuc4/claude/SiteAntoine && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Components accept optional props with fallbacks, page.tsx fetches global and passes data, seed script ready to run, TypeScript compiles</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. Seed runs: `pnpm seed:accueil` (requires DB connection)
3. Visual: dev server shows homepage with same content as before (fallbacks active until seed runs)
4. After seed: admin panel shows "Accueil" global under "Pages" group with 5 editable fields
</verification>

<success_criteria>
- Global Accueil exists in Payload admin with 5 fields
- HeroSection, presentation section, and CTABandeau render data from the global
- Fallback text matches current hardcoded values (no visual regression before seed)
- Seed script pre-fills default values
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260405-8ah-creer-global-accueil-avec-herotitre-hero/260405-8ah-SUMMARY.md`
</output>
