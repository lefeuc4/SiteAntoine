---
phase: 01-fondation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: false
requirements:
  - DSGN-01

must_haves:
  truths:
    - "La palette Energie & Performance est definie dans le design system Tailwind"
    - "Les polices Montserrat (headings) et Inter (body) sont chargees via next/font sans layout shift"
    - "Le root layout a lang=fr sur la balise html"
    - "La page placeholder affiche le texte en Montserrat (heading) et Inter (body) sur fond blanc-pur"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Design system tokens — palette, fonts, transitions via @theme directive"
      contains: "--color-bleu-nuit"
    - path: "src/app/layout.tsx"
      provides: "Root layout avec fonts Montserrat/Inter via next/font, lang=fr, body classes"
      contains: "lang=\"fr\""
    - path: "src/app/page.tsx"
      provides: "Placeholder page using design system tokens"
      contains: "Site en cours de construction"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals"
    - from: "src/app/globals.css"
      to: "layout.tsx font variables"
      via: "@theme references --font-montserrat and --font-inter CSS variables"
      pattern: "--font-heading.*--font-montserrat"
---

<objective>
Configure the Tailwind CSS v4 design system with the "Energie & Performance" palette, load Montserrat and Inter fonts via next/font, and create the placeholder home page — establishing the visual identity foundation for all subsequent phases.

Purpose: DSGN-01 requires a new modern visual identity. This plan defines the tokens that Phase 2+ will consume.
Output: globals.css with @theme tokens, layout.tsx with fonts, placeholder page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fondation/01-CONTEXT.md
@.planning/phases/01-fondation/01-RESEARCH.md
@.planning/phases/01-fondation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Tailwind v4 design system tokens in globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    src/app/globals.css (current state from Payload template — must see existing content before modifying)
    .planning/phases/01-fondation/01-UI-SPEC.md (Color section, Typography section, Animations section — exact token values)
    .planning/phases/01-fondation/01-RESEARCH.md (Pattern 4: Design System Tailwind CSS v4)
  </read_first>
  <action>
    Replace the contents of `src/app/globals.css` with the complete design system. Keep any Payload-required CSS imports that exist in the current file (read it first to check).

    The file must contain exactly:

    ```css
    @import "tailwindcss";

    @theme {
      /* Palette "Energie & Performance" (D-02) */
      --color-blanc-pur: #F8FAFC;
      --color-bleu-nuit: #0F172A;
      --color-bleu-electrique: #3B82F6;
      --color-vert-energie: #10B981;
      --color-gris-ardoise: #64748B;
      --color-rouge-erreur: #EF4444;

      /* Typographie (D-06, D-07) */
      --font-heading: var(--font-montserrat), 'Montserrat', sans-serif;
      --font-body: var(--font-inter), 'Inter', sans-serif;

      /* Font sizes — 4 roles only */
      --font-size-sm: 0.875rem;   /* 14px — label */
      --font-size-base: 1rem;     /* 16px — body */
      --font-size-xl: 1.75rem;    /* 28px — heading */
      --font-size-4xl: 2.25rem;   /* 36px — display */

      /* Animation timing (D-05) */
      --transition-fast: 150ms ease;
      --transition-base: 200ms ease;
      --transition-slow: 300ms ease-out;
    }
    ```

    IMPORTANT: If the Payload template includes additional CSS (e.g., for admin styles), preserve those imports/rules ABOVE the @theme block. The @theme block is additive — it does not conflict with Payload's admin CSS.

    Do NOT import Google Fonts via CSS @import — fonts are loaded via next/font in layout.tsx.
  </action>
  <verify>
    <automated>grep -q "\-\-color-bleu-nuit: #0F172A" /home/lefeuc4/claude/SiteAntoine/src/app/globals.css && grep -q "\-\-color-bleu-electrique: #3B82F6" /home/lefeuc4/claude/SiteAntoine/src/app/globals.css && grep -q "\-\-color-vert-energie: #10B981" /home/lefeuc4/claude/SiteAntoine/src/app/globals.css && grep -q "\-\-font-heading" /home/lefeuc4/claude/SiteAntoine/src/app/globals.css && grep -q "\-\-transition-fast" /home/lefeuc4/claude/SiteAntoine/src/app/globals.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `@import "tailwindcss"`
    - globals.css contains `@theme` block with exactly these color tokens: --color-blanc-pur (#F8FAFC), --color-bleu-nuit (#0F172A), --color-bleu-electrique (#3B82F6), --color-vert-energie (#10B981), --color-gris-ardoise (#64748B), --color-rouge-erreur (#EF4444)
    - globals.css contains --font-heading referencing --font-montserrat
    - globals.css contains --font-body referencing --font-inter
    - globals.css contains --font-size-sm (0.875rem), --font-size-base (1rem), --font-size-xl (1.75rem), --font-size-4xl (2.25rem)
    - globals.css contains --transition-fast (150ms), --transition-base (200ms), --transition-slow (300ms)
    - globals.css does NOT contain any Google Fonts @import URL
  </acceptance_criteria>
  <done>Tailwind v4 @theme block contains all 6 color tokens, 2 font families, 4 font sizes, and 3 transition timings matching the UI-SPEC exactly</done>
</task>

<task type="auto">
  <name>Task 2: Configure root layout with fonts and create placeholder page</name>
  <files>src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    src/app/layout.tsx (current state from Payload template — critical to see existing structure, metadata, imports)
    src/app/page.tsx (current state from Payload template)
    src/app/globals.css (after Task 1 — verify @theme tokens are in place)
    .planning/phases/01-fondation/01-UI-SPEC.md (Typography section for font config, Copywriting Contract for placeholder text, Implementation Checklist)
    .planning/phases/01-fondation/01-RESEARCH.md (Pattern 4 for layout.tsx example)
  </read_first>
  <action>
    1. Update `src/app/layout.tsx`:
       - KEEP any existing Payload-required imports, metadata, or wrappers (read the file first to understand the existing structure)
       - ADD font imports from next/font/google:
         ```typescript
         import { Montserrat, Inter } from 'next/font/google'

         const montserrat = Montserrat({
           subsets: ['latin'],
           weight: ['700', '900'],
           variable: '--font-montserrat',
           display: 'swap',
         })

         const inter = Inter({
           subsets: ['latin'],
           weight: ['400'],
           variable: '--font-inter',
           display: 'swap',
         })
         ```
       - ENSURE the `<html>` tag has `lang="fr"` (per Pitfall 5 in RESEARCH.md)
       - ADD font CSS variable classes to `<html>`: `className={`${montserrat.variable} ${inter.variable}`}`
       - ADD body classes: `className="font-body bg-blanc-pur text-bleu-nuit"`
       - ENSURE `import './globals.css'` is present (should already exist from template)

    2. Update `src/app/page.tsx` with the placeholder page (per UI-SPEC Copywriting Contract):
       ```tsx
       export default function Home() {
         return (
           <main className="flex min-h-screen flex-col items-center justify-center px-4">
             <h1 className="font-heading text-4xl font-bold text-bleu-nuit mb-4">
               Site en cours de construction
             </h1>
             <p className="font-body text-base text-gris-ardoise text-center max-w-md">
               Le site d&apos;Antoine Profit, coach bien-etre, arrive bientot.
             </p>
           </main>
         )
       }
       ```
       Use `text-4xl` for the heading (maps to --font-size-4xl via @theme), `text-base` for body.
       Use `font-heading` (Montserrat) for h1, `font-body` (Inter) for paragraph.
       Use `text-bleu-nuit` for heading, `text-gris-ardoise` for body text, implicit `bg-blanc-pur` from body.

    3. Verify `pnpm run dev` still starts and the placeholder page renders at http://localhost:3000.
  </action>
  <verify>
    <automated>grep -q 'lang="fr"' /home/lefeuc4/claude/SiteAntoine/src/app/layout.tsx && grep -q "font-montserrat" /home/lefeuc4/claude/SiteAntoine/src/app/layout.tsx && grep -q "font-inter" /home/lefeuc4/claude/SiteAntoine/src/app/layout.tsx && grep -q "font-body bg-blanc-pur text-bleu-nuit" /home/lefeuc4/claude/SiteAntoine/src/app/layout.tsx && grep -q "Site en cours de construction" /home/lefeuc4/claude/SiteAntoine/src/app/page.tsx && grep -q "font-heading" /home/lefeuc4/claude/SiteAntoine/src/app/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx contains `import { Montserrat, Inter } from 'next/font/google'`
    - layout.tsx contains `variable: '--font-montserrat'` with weights ['700', '900']
    - layout.tsx contains `variable: '--font-inter'` with weight ['400']
    - layout.tsx contains `lang="fr"` on the html element
    - layout.tsx contains `${montserrat.variable}` and `${inter.variable}` on html className
    - layout.tsx contains `font-body bg-blanc-pur text-bleu-nuit` on body className
    - layout.tsx contains `import './globals.css'`
    - page.tsx contains heading text "Site en cours de construction" with class "font-heading"
    - page.tsx contains body text "Antoine Profit, coach bien-etre, arrive bientot" with class "text-gris-ardoise"
  </acceptance_criteria>
  <done>Root layout loads Montserrat + Inter via next/font with lang="fr", body uses design system classes, placeholder page renders with correct fonts and colors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify visual identity in browser</name>
  <what-built>
    Complete Tailwind v4 design system with "Energie & Performance" palette, Montserrat/Inter fonts loaded via next/font, and placeholder home page.
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000 in your browser
    2. Verify the page background is light (#F8FAFC blanc-pur) — NOT white (#FFFFFF)
    3. Verify "Site en cours de construction" renders in Montserrat Bold (thick, impactful letters)
    4. Verify the body text renders in Inter Regular (clean, thinner letters — different from heading)
    5. Open browser DevTools (F12) and inspect the html element:
       - Confirm `lang="fr"` attribute is present
       - Confirm CSS variables `--font-montserrat` and `--font-inter` are defined
    6. Inspect the body element: confirm it has classes `font-body bg-blanc-pur text-bleu-nuit`
    7. Navigate to http://localhost:3000/admin — confirm the admin UI still works after layout changes
  </how-to-verify>
  <resume-signal>Type "approved" if fonts and colors display correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- globals.css contains @theme with all 6 colors, 2 font families, 4 font sizes, 3 transitions
- layout.tsx loads Montserrat (700, 900) + Inter (400) via next/font/google
- layout.tsx has lang="fr" on html, font-body bg-blanc-pur text-bleu-nuit on body
- page.tsx shows placeholder text using font-heading and text-gris-ardoise
- Dev server starts without error
- Admin at /admin still functional
</verification>

<success_criteria>
- All design tokens from UI-SPEC are implemented in globals.css @theme block
- Montserrat and Inter fonts load without FOUT/FOIT (display: swap + next/font)
- Placeholder page visually confirms the palette works (blanc-pur background, bleu-nuit heading, gris-ardoise body)
- lang="fr" present on html element for SEO/accessibility
- No regression on Payload admin functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-fondation/01-02-SUMMARY.md`
</output>
