---
phase: 02-pages-publiques
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/app/page.tsx
  - src/components/sections/HeroSection.tsx
  - src/components/sections/ServicesApercu.tsx
  - src/components/sections/ResultatsVedette.tsx
  - src/components/sections/CTABandeau.tsx
  - src/app/mon-histoire/page.tsx
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02

must_haves:
  truths:
    - "Accueil page displays hero with headline, subheading, and CTA button"
    - "Accueil page shows 3-4 service apercu cards below hero"
    - "Accueil page shows a presentation paragraph with Mon Histoire link"
    - "Accueil page shows 2-3 featured result cards"
    - "Accueil page shows a full-width dark CTA bandeau at the bottom"
    - "Mon Histoire page displays a vertical timeline with chronological steps"
    - "Both pages consume data from Payload local API via getPayload()"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Accueil page with hero + 4 sections"
      contains: "getPayload"
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Hero split layout with headline and CTA"
      contains: "Transforme ton corps"
    - path: "src/components/sections/ServicesApercu.tsx"
      provides: "Service preview cards grid"
    - path: "src/components/sections/ResultatsVedette.tsx"
      provides: "Featured results cards"
    - path: "src/components/sections/CTABandeau.tsx"
      provides: "Full-width dark CTA section"
      contains: "Prendre contact"
    - path: "src/app/mon-histoire/page.tsx"
      provides: "Timeline page with chronological steps"
      contains: "getPayload"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/sections/HeroSection.tsx"
      via: "import and render as first section"
      pattern: "import HeroSection"
    - from: "src/app/page.tsx"
      to: "payload local API"
      via: "getPayload({ config }) for services-apercu and resultats"
      pattern: "payload.find"
    - from: "src/app/mon-histoire/page.tsx"
      to: "payload local API"
      via: "getPayload({ config }) for mon-histoire PageContent"
      pattern: "collection: 'page-content'"
---

<objective>
Build the Accueil (home) page with hero split layout and 4 content sections, and the Mon Histoire page with vertical timeline.

Purpose: The Accueil page is the first impression — hero + services + results + CTA convince visitors in seconds. Mon Histoire builds trust through Antoine's personal journey timeline.
Output: Fully functional Accueil page at `/` and Mon Histoire page at `/mon-histoire`, both consuming Payload data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-pages-publiques/02-CONTEXT.md
@.planning/phases/02-pages-publiques/02-UI-SPEC.md
@.planning/phases/02-pages-publiques/02-01-SUMMARY.md

<interfaces>
<!-- Components available from Plan 01 -->
From src/components/layout/Header.tsx:
```typescript
// Client component with props:
interface HeaderProps { transparentOnLoad?: boolean }
// Default: transparentOnLoad=false
```

From src/components/ui/ScrollReveal.tsx:
```typescript
export default function ScrollReveal({ children, className }: { children: React.ReactNode; className?: string })
```

From src/components/ui/ServiceCard.tsx:
```typescript
interface ServiceCardProps { titre: string; description: string; icone: string }
```

From src/components/ui/ResultatCard.tsx:
```typescript
interface ResultatCardProps { prenomClient: string; citation?: string | null; programmeSuivi?: string | null; duree?: string | null; photoAvantUrl: string; photoApresUrl: string }
```

<!-- Payload data patterns -->
PageContent (slug: 'page-content'):
  - page: select, section: text, titre: text, contenu: richText, image: upload
  - Query: payload.find({ collection: 'page-content', where: { page: { equals: 'accueil' }, section: { equals: 'services-apercu' } } })
  - For mon-histoire: payload.find({ collection: 'page-content', where: { page: { equals: 'mon-histoire' }, section: { equals: 'mon-histoire' } } })

Resultats (slug: 'resultats'):
  - Query: payload.find({ collection: 'resultats', limit: 3, depth: 2 })
  - photosAvant/photosApres are arrays of { photo: { url: string } } when depth >= 2
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Accueil page with hero and 4 content sections</name>
  <files>src/app/page.tsx, src/components/sections/HeroSection.tsx, src/components/sections/ServicesApercu.tsx, src/components/sections/ResultatsVedette.tsx, src/components/sections/CTABandeau.tsx</files>
  <read_first>
    - src/app/page.tsx (current placeholder page — will be replaced)
    - src/components/ui/ServiceCard.tsx (props interface for ServiceCard)
    - src/components/ui/ResultatCard.tsx (props interface for ResultatCard)
    - src/components/ui/ScrollReveal.tsx (wrapper component available)
    - src/components/layout/Header.tsx (check transparentOnLoad prop name)
    - .planning/phases/02-pages-publiques/02-UI-SPEC.md (Page Accueil contract, Copywriting contract)
  </read_first>
  <action>
1. Create `src/components/sections/HeroSection.tsx` — Server Component:
   - Full-width section with `py-16 lg:py-0` (no vertical padding on desktop — hero is full viewport height)
   - Desktop: flex row, 50/50 split. Left side: `flex-1 flex flex-col justify-center px-8 lg:px-12 max-w-[640px]`
     - H1: `"Transforme ton corps. Transforme ta vie."` — `text-4xl font-heading font-bold text-bleu-nuit leading-tight`
     - Subheading: `"Coach bien-etre specialise en transformation physique et mentale. Un accompagnement personnalise pour atteindre tes objectifs."` — `text-base font-body text-gris-ardoise mt-4 mb-8`
     - Primary CTA: `<Link href="/contact">` with `bg-bleu-electrique text-blanc-pur rounded-full px-6 py-3 font-heading text-sm inline-block hover:brightness-110 transition` style `transitionDuration: 'var(--transition-fast)'`. Text: "Prendre contact"
     - Secondary CTA: `<Link href="/programmes">` with `text-bleu-electrique font-heading text-sm ml-4 inline-block hover:underline`. Text: "Voir les programmes"
   - Right side: `flex-1 relative min-h-[200px] lg:min-h-screen` with placeholder image block:
     - `<div className="w-full h-full bg-gris-ardoise/30 flex items-center justify-center"><span className="text-gris-ardoise text-sm">Photo coaching a venir</span></div>`
   - Mobile: stacked — image first (200px height, full width), then text block below

2. Create `src/components/sections/ServicesApercu.tsx` — Server Component:
   - Props: `services: Array<{ titre: string; contenu: string; icone: string }>`
   - Note: `icone` is not stored in PageContent schema — hardcode an icon map by index: index 0 → 'heart', index 1 → 'users', index 2 → 'zap', index 3 → 'target'. This maps to the seed data order: Coaching Individuel → heart, Programmes de Groupe → users, Suivi Nutritionnel → zap, Bilan Complet → target
   - Section: `py-12 lg:py-16 px-8 lg:px-12 max-w-[1280px] mx-auto`
   - Title: none (cards speak for themselves per D-06)
   - Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6`
   - Each item renders `<ScrollReveal><ServiceCard titre={s.titre} description={s.contenu} icone={iconMap[i]} /></ScrollReveal>`

3. Create `src/components/sections/ResultatsVedette.tsx` — Server Component:
   - Props: `resultats: Array<{ prenomClient: string; citation?: string | null; programmeSuivi?: string | null; duree?: string | null; photoAvantUrl: string; photoApresUrl: string }>`
   - Section: `py-12 lg:py-16 px-8 lg:px-12 max-w-[1280px] mx-auto`
   - Title: `"Resultats & Temoignages"` — `text-xl font-heading font-bold text-bleu-nuit mb-8 text-center`
   - Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
   - Each result renders `<ScrollReveal><ResultatCard ... /></ScrollReveal>`

4. Create `src/components/sections/CTABandeau.tsx` — Server Component:
   - Full-width: `bg-bleu-nuit py-12 lg:py-16 px-8`
   - Content centered: `max-w-[1280px] mx-auto text-center`
   - Headline: `"Pret a transformer ta vie ?"` — `text-xl lg:text-4xl font-heading font-bold text-blanc-pur mb-6`
   - CTA button: `<Link href="/contact">` with `bg-bleu-electrique text-blanc-pur rounded-full px-8 py-4 font-heading text-base inline-block hover:brightness-110 transition` style `transitionDuration: 'var(--transition-fast)'`. Text: "Prendre contact"

5. Replace `src/app/page.tsx` — async Server Component:
   - Export metadata: `{ title: "Antoine Profit — Coach Bien-Etre" }`
   - Fetch services data: `payload.find({ collection: 'page-content', where: { page: { equals: 'accueil' }, section: { equals: 'services-apercu' } } })`
   - Fetch featured resultats: `payload.find({ collection: 'resultats', limit: 3, depth: 2 })`
   - Extract plain text from richText `contenu` field for ServiceCard description — use a helper: if `contenu?.root?.children?.[0]?.children?.[0]?.text` exists, use it, otherwise fallback to empty string. This avoids importing the full RichText component for a simple apercu.
   - Map resultats to extract `photoAvantUrl` and `photoApresUrl`: access `resultat.photosAvant?.[0]?.photo?.url || ''` and `resultat.photosApres?.[0]?.photo?.url || ''`
   - Render order: `<HeroSection />` → `<ScrollReveal><ServicesApercu services={...} /></ScrollReveal>` → Presentation section (inline) → `<ResultatsVedette resultats={...} />` → `<CTABandeau />`
   - Presentation section (between services and results): `<section className="py-12 lg:py-16 px-8 lg:px-12 max-w-[1280px] mx-auto"><ScrollReveal><p className="text-base font-body text-gris-ardoise max-w-2xl">Coach passionne depuis plus de 10 ans, j'accompagne des femmes et des hommes dans leur transformation physique et mentale. Mon approche allie entrainement personnalise, nutrition adaptee et suivi bienveillant. <Link href="/mon-histoire" className="text-bleu-electrique hover:underline font-heading">Decouvrir mon parcours →</Link></p></ScrollReveal></section>`
   - IMPORTANT: The Accueil page needs the Header to be transparent on load. Since Header is in root layout with `transparentOnLoad={false}` default, we need a different approach. Option: modify the root layout to pass `transparentOnLoad` based on pathname. Simpler approach: use CSS — add a `<div data-hero-page>` wrapper and use CSS to make the header transparent when the page has this attribute. ACTUALLY simplest: modify layout.tsx in this task to use a client wrapper that reads pathname. BUT that makes the entire layout a client component. BEST approach: keep Header default as `transparentOnLoad={false}` and in the Accueil page, render a `<style>` tag or use a class on the page container. SIMPLEST: just accept that Header starts solid white on all pages including Accueil. The transparent-on-hero effect requires the Header to know it's on the hero page. Since Header is already a client component using `usePathname()`, add logic in Header.tsx: `const isHeroPage = pathname === '/'` and use that instead of the `transparentOnLoad` prop. This is the cleanest solution — no prop drilling needed. Update Header.tsx to add: `const pathname = usePathname(); const isHeroPage = pathname === '/'` and change the background logic to: `isHeroPage && !scrolled ? 'bg-transparent' : 'bg-blanc-pur shadow-sm'`. Also toggle text color: `isHeroPage && !scrolled ? 'text-blanc-pur' : 'text-bleu-nuit'` for logo and nav links. Remove the `transparentOnLoad` prop since it's no longer needed.
  </action>
  <verify>
    <automated>cd /home/lefeuc4/claude/SiteAntoine && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx` contains `getPayload`
    - `src/app/page.tsx` contains `collection: 'page-content'`
    - `src/app/page.tsx` contains `collection: 'resultats'`
    - `src/app/page.tsx` contains `import HeroSection`
    - `src/app/page.tsx` contains `import ServicesApercu`
    - `src/app/page.tsx` contains `import ResultatsVedette`
    - `src/app/page.tsx` contains `import CTABandeau`
    - `src/app/page.tsx` contains `Antoine Profit` in metadata title
    - `src/components/sections/HeroSection.tsx` contains `Transforme ton corps`
    - `src/components/sections/HeroSection.tsx` contains `Prendre contact`
    - `src/components/sections/HeroSection.tsx` contains `Voir les programmes`
    - `src/components/sections/ServicesApercu.tsx` contains `ServiceCard`
    - `src/components/sections/ResultatsVedette.tsx` contains `ResultatCard`
    - `src/components/sections/CTABandeau.tsx` contains `bg-bleu-nuit`
    - `src/components/sections/CTABandeau.tsx` contains `Prendre contact`
    - `src/components/layout/Header.tsx` contains `pathname === '/'` or equivalent hero page detection
    - `pnpm build` completes without error
  </acceptance_criteria>
  <done>Accueil page renders hero with CTA, 4 service preview cards from Payload, presentation paragraph, 3 featured results, and dark CTA bandeau. Header is transparent on Accueil hero and solid on all other pages.</done>
</task>

<task type="auto">
  <name>Task 2: Create Mon Histoire page with vertical timeline</name>
  <files>src/app/mon-histoire/page.tsx</files>
  <read_first>
    - src/collections/PageContent.ts (field names: page, section, titre, contenu)
    - src/components/ui/ScrollReveal.tsx (wrapper component)
    - .planning/phases/02-pages-publiques/02-UI-SPEC.md (Mon Histoire timeline contract)
    - .planning/phases/02-pages-publiques/02-UI-SPEC.md (Copywriting — Mon Histoire H1 and title)
  </read_first>
  <action>
1. Create directory `src/app/mon-histoire/` if not exists.

2. Create `src/app/mon-histoire/page.tsx` — async Server Component:
   - Export metadata: `{ title: "Mon Histoire — Antoine Profit" }`
   - Fetch timeline data: `payload.find({ collection: 'page-content', where: { page: { equals: 'mon-histoire' }, section: { equals: 'mon-histoire' } } })`
   - Extract plain text from richText `contenu` field for each timeline entry (same helper as Accueil — `contenu?.root?.children?.[0]?.children?.[0]?.text || ''`)
   - Main container: `max-w-[1280px] mx-auto px-8 lg:px-12 py-12 lg:py-16`
   - H1: `"Mon Parcours"` — `text-4xl font-heading font-bold text-bleu-nuit mb-12 text-center`
   - Empty state: if no docs returned, render `<p className="text-gris-ardoise text-center">Parcours en cours de redaction.</p>`
   - Timeline container: `relative` with vertical line `before:absolute before:left-1/2 before:top-0 before:bottom-0 before:w-0.5 before:bg-bleu-electrique before:-translate-x-1/2` (desktop). On mobile: `before:left-4`
   - Each timeline entry (mapped from docs):
     - Wrapper: `relative flex items-start gap-8 mb-16` 
     - Desktop alternating: even index → `flex-row-reverse` (content right of line), odd index → `flex-row` (content left of line). On mobile: always left-to-right (content right of dot)
     - Dot: `absolute left-1/2 -translate-x-1/2 w-3 h-3 rounded-full bg-bleu-electrique border-2 border-blanc-pur z-10` (desktop). On mobile: `left-4`
     - Content block: `w-[calc(50%-2rem)]` on desktop, `ml-12 w-full` on mobile
     - Titre contains year + label (e.g., "2010 — Les Premiers Pas"): render as `<h3 className="text-xl font-heading font-bold text-bleu-nuit mb-2">{titre}</h3>`
     - Text: `<p className="text-base font-body text-gris-ardoise">{extracted plain text}</p>`
     - Wrap each entry in `<ScrollReveal>`
   - Responsive approach: Use Tailwind responsive classes. On mobile (`default`), all entries are positioned to the right of a left-aligned line. On desktop (`lg:`), entries alternate left/right of a center line.
   - Simplified mobile implementation: On mobile, hide the alternating layout. Use a single-column approach:
     ```
     <div className="relative pl-12 lg:pl-0">
       <!-- Mobile: vertical line on left -->
       <div className="absolute left-4 top-0 bottom-0 w-0.5 bg-bleu-electrique lg:left-1/2 lg:-translate-x-1/2" />
       {entries.map((entry, i) => (
         <ScrollReveal key={entry.id}>
           <div className="relative mb-16 lg:flex lg:items-start lg:gap-8">
             <!-- Dot -->
             <div className="absolute left-[-2rem] top-1 w-3 h-3 rounded-full bg-bleu-electrique border-2 border-blanc-pur lg:left-1/2 lg:-translate-x-1/2 lg:top-2 z-10" />
             <!-- Content -->
             <div className={`lg:w-[calc(50%-2rem)] ${i % 2 === 0 ? 'lg:mr-auto lg:pr-8 lg:text-right' : 'lg:ml-auto lg:pl-8'}`}>
               <h3 className="text-xl font-heading font-bold text-bleu-nuit mb-2">{entry.titre}</h3>
               <p className="text-base font-body text-gris-ardoise">{plainText}</p>
             </div>
           </div>
         </ScrollReveal>
       ))}
     </div>
     ```
  </action>
  <verify>
    <automated>cd /home/lefeuc4/claude/SiteAntoine && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/mon-histoire/page.tsx` contains `getPayload`
    - `src/app/mon-histoire/page.tsx` contains `collection: 'page-content'`
    - `src/app/mon-histoire/page.tsx` contains `section: { equals: 'mon-histoire' }`
    - `src/app/mon-histoire/page.tsx` contains `Mon Parcours`
    - `src/app/mon-histoire/page.tsx` contains `Mon Histoire — Antoine Profit` in metadata
    - `src/app/mon-histoire/page.tsx` contains `Parcours en cours de redaction` (empty state)
    - `src/app/mon-histoire/page.tsx` contains `bg-bleu-electrique` (timeline dot/line)
    - `src/app/mon-histoire/page.tsx` contains `ScrollReveal`
    - `pnpm build` completes without error
  </acceptance_criteria>
  <done>Mon Histoire page renders a vertical timeline with alternating entries on desktop and left-aligned entries on mobile. Data comes from Payload PageContent collection filtered by section 'mon-histoire'. Empty state message displayed when no data. Each entry is wrapped in ScrollReveal for scroll animation.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with zero errors
- Accueil page at `/` renders hero, services, presentation, results, and CTA sections
- Mon Histoire page at `/mon-histoire` renders timeline
- Both pages fetch data from Payload local API
- Header is transparent on Accueil, solid on Mon Histoire
</verification>

<success_criteria>
Accueil page displays hero split layout with "Transforme ton corps. Transforme ta vie." headline, 4 service cards, presentation paragraph, 3 featured results, and CTA bandeau. Mon Histoire page displays vertical timeline with chronological entries. Both pages consume Payload data and the Next.js build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pages-publiques/02-03-SUMMARY.md`
</output>
