---
phase: 02-pages-publiques
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/components/ui/ScrollReveal.tsx
  - src/components/ui/BeforeAfterSlider.tsx
  - src/components/ui/ServiceCard.tsx
  - src/components/ui/ProgrammeCard.tsx
  - src/components/ui/ResultatCard.tsx
autonomous: true
requirements:
  - PAGE-06
  - PAGE-07
  - DSGN-02

must_haves:
  truths:
    - "Navigation with 5 links is visible on every page via Header in root layout"
    - "CTA contact button (Me contacter) is always visible in the header"
    - "Footer displays on every page with logo, nav links, contact link, mentions legales"
    - "Header is transparent on load (for hero pages) and transitions to solid on scroll"
    - "Mobile hamburger menu opens full-screen overlay with nav links"
    - "ScrollReveal wrapper animates elements on viewport entry"
    - "BeforeAfterSlider renders a draggable before/after image comparison"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Sticky header with scroll detection and mobile hamburger"
      contains: "use client"
    - path: "src/components/layout/Footer.tsx"
      provides: "Minimal footer with nav links and contact"
    - path: "src/components/ui/ScrollReveal.tsx"
      provides: "IntersectionObserver scroll reveal wrapper"
      contains: "use client"
    - path: "src/components/ui/BeforeAfterSlider.tsx"
      provides: "Before/after image comparison slider"
      contains: "use client"
    - path: "src/components/ui/ServiceCard.tsx"
      provides: "Service card component for Mes Services and Accueil apercu"
    - path: "src/components/ui/ProgrammeCard.tsx"
      provides: "Programme card with duration badge and objectives"
    - path: "src/components/ui/ResultatCard.tsx"
      provides: "Result card with BeforeAfterSlider, citation, and meta"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Header.tsx"
      via: "import and render in body"
      pattern: "import Header from"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Footer.tsx"
      via: "import and render in body"
      pattern: "import Footer from"
    - from: "src/components/ui/ResultatCard.tsx"
      to: "src/components/ui/BeforeAfterSlider.tsx"
      via: "import and render inside card"
      pattern: "import BeforeAfterSlider from"
---

<objective>
Create the shared layout shell (Header, Footer, root layout integration) and all reusable UI components needed by the 5 public pages.

Purpose: Establishes the navigation, persistent contact CTA, scroll reveal animation, before/after slider, and card components that every subsequent page plan depends on.
Output: Header.tsx, Footer.tsx, ScrollReveal.tsx, BeforeAfterSlider.tsx, ServiceCard.tsx, ProgrammeCard.tsx, ResultatCard.tsx — all importable and ready for page integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pages-publiques/02-CONTEXT.md
@.planning/phases/02-pages-publiques/02-RESEARCH.md
@.planning/phases/02-pages-publiques/02-UI-SPEC.md

<interfaces>
<!-- Existing design tokens from globals.css -->
Colors: bg-blanc-pur (#F8FAFC), bg-bleu-nuit (#0F172A), bg-bleu-electrique (#3B82F6), bg-vert-energie (#10B981), text-gris-ardoise (#64748B), text-rouge-erreur (#EF4444)
Fonts: font-heading (Montserrat), font-body (Inter)
Sizes: text-sm (14px), text-base (16px), text-xl (28px), text-4xl (36px)
Transitions: --transition-fast (150ms), --transition-base (200ms), --transition-slow (300ms)

<!-- Existing root layout pattern -->
From src/app/layout.tsx:
```typescript
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="fr" className={`${montserrat.variable} ${inter.variable}`}>
      <body className="font-body bg-blanc-pur text-bleu-nuit">{children}</body>
    </html>
  )
}
```

<!-- Payload collection types needed for card props -->
From src/collections/Programmes.ts:
Fields: titre (text, required), description (richText, required), duree (text, required), objectifs (textarea), publicCible (textarea), imageCouverture (upload -> media), ordre (number)

From src/collections/Resultats.ts:
Fields: prenomClient (text, required), histoire (richText, required), programmeSuivi (text), duree (text), citation (textarea), consentementCNIL (checkbox, required), photosAvant (array of upload -> media), photosApres (array of upload -> media)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lucide-react, add scroll reveal CSS, create Header and Footer</name>
  <files>package.json, src/app/globals.css, src/components/layout/Header.tsx, src/components/layout/Footer.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/app/globals.css (current design tokens — must append, not overwrite)
    - src/app/layout.tsx (current root layout — must modify to add Header/Footer)
    - package.json (current deps — verify lucide-react not already installed)
    - .planning/phases/02-pages-publiques/02-UI-SPEC.md (Header and Footer component contracts)
  </read_first>
  <action>
1. Run `pnpm add lucide-react` to install icon library.

2. Append scroll reveal CSS classes to the END of `src/app/globals.css` (after the existing @theme block):
```css
.reveal {
  opacity: 0;
  transform: translateY(1rem);
  transition: opacity var(--transition-slow), transform var(--transition-slow);
}
.reveal.revealed {
  opacity: 1;
  transform: translateY(0);
}
```

3. Create `src/components/layout/Header.tsx` — Client Component (`'use client'`):
   - Props: `transparentOnLoad?: boolean` (default: `false`) — per Research Pitfall 2, only page Accueil passes `true`
   - State: `scrolled` (boolean, tracks `window.scrollY > 10`), `menuOpen` (boolean, hamburger toggle)
   - `useEffect` with scroll listener (passive: true), sets `scrolled` state
   - Sticky positioning: `sticky top-0 z-50`
   - Background transition: when `transparentOnLoad && !scrolled` → `bg-transparent`, otherwise → `bg-blanc-pur shadow-sm`. Transition uses `style={{ transitionDuration: 'var(--transition-slow)' }}`
   - Desktop layout (hidden below `lg`): Logo text "Antoine Profit" left (`font-heading text-xl font-bold`), 5 nav links centered (`next/link`): Accueil `/`, Mon Histoire `/mon-histoire`, Mes Services `/services`, Les Programmes `/programmes`, Resultats `/resultats`. CTA button right: "Me contacter" `bg-bleu-electrique text-blanc-pur rounded-full px-5 py-2 text-sm font-heading` linking to `/contact`
   - Mobile layout (visible below `lg`): Logo left, hamburger button right (44px min touch target, `aria-label="Ouvrir le menu"` / `aria-label="Fermer le menu"` toggled). Use `Menu` and `X` icons from `lucide-react` (size 24)
   - Hamburger overlay: when `menuOpen`, render full-screen fixed overlay `bg-bleu-nuit/95 inset-0 z-40` with nav links centered vertically (`text-blanc-pur text-xl font-heading`), each link has `min-h-[44px]`. Close on link click (setMenuOpen(false)). Animation: opacity 0→1 + scale 0.97→1, duration `var(--transition-base)`
   - Active page indicator: use `usePathname()` from `next/navigation` to highlight current nav link with `text-bleu-electrique` (desktop) or underline (mobile)
   - Text color: when transparent background → `text-blanc-pur` for logo and nav links (hero has dark image placeholder behind). When solid background → `text-bleu-nuit`. CTA button always `text-blanc-pur bg-bleu-electrique`
   - Nav items on hero transparent: white text with `text-blanc-pur` class. On scroll/non-hero pages: `text-bleu-nuit`

4. Create `src/components/layout/Footer.tsx` — Server Component (no `'use client'`):
   - `bg-bleu-nuit text-blanc-pur` full-width
   - Padding: `py-12 lg:py-16 px-8 lg:px-12`
   - Max-width container: `max-w-[1280px] mx-auto`
   - Three-zone layout (flex row on desktop, stacked on mobile):
     - Left: "Antoine Profit" logo text (`font-heading text-xl font-bold`) + tagline "Coach Bien-Etre" (`text-sm text-gris-ardoise`)
     - Center: Nav links (same 5 as header) as `next/link`, `text-sm text-gris-ardoise hover:text-blanc-pur` transition
     - Right: "Me contacter" link (`text-bleu-electrique text-sm font-heading`) linking to `/contact`
   - Bottom bar: separator line (`border-t border-gris-ardoise/30 mt-8 pt-4`), flex row: "Mentions legales" link (`/mentions-legales`, `text-sm text-gris-ardoise`) left, copyright "© 2026 Antoine Profit" right (`text-sm text-gris-ardoise`)

5. Update `src/app/layout.tsx`:
   - Add imports: `import Header from '@/components/layout/Header'` and `import Footer from '@/components/layout/Footer'`
   - Wrap `{children}` in body: `<Header /><main className="min-h-screen">{children}</main><Footer />`
   - Keep ALL existing code (fonts, metadata, html lang, body className)
   - Note: Header gets `transparentOnLoad={false}` by default — individual pages do NOT need to pass this prop. Only Accueil page will override this (handled in Plan 03)
  </action>
  <verify>
    <automated>cd /home/lefeuc4/claude/SiteAntoine && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"lucide-react"` in dependencies
    - `src/app/globals.css` contains `.reveal {` and `.reveal.revealed {`
    - `src/components/layout/Header.tsx` contains `'use client'` on line 1
    - `src/components/layout/Header.tsx` contains `transparentOnLoad`
    - `src/components/layout/Header.tsx` contains `usePathname`
    - `src/components/layout/Header.tsx` contains `aria-label`
    - `src/components/layout/Header.tsx` contains `menuOpen`
    - `src/components/layout/Header.tsx` contains `passive: true`
    - `src/components/layout/Header.tsx` contains links to `/mon-histoire`, `/services`, `/programmes`, `/resultats`
    - `src/components/layout/Header.tsx` contains `Me contacter`
    - `src/components/layout/Footer.tsx` does NOT contain `'use client'`
    - `src/components/layout/Footer.tsx` contains `bg-bleu-nuit`
    - `src/components/layout/Footer.tsx` contains `Mentions legales`
    - `src/components/layout/Footer.tsx` contains `© 2026 Antoine Profit`
    - `src/app/layout.tsx` contains `import Header from`
    - `src/app/layout.tsx` contains `import Footer from`
    - `src/app/layout.tsx` contains `<Header`
    - `src/app/layout.tsx` contains `<Footer`
    - `pnpm build` completes without error
  </acceptance_criteria>
  <done>Header with sticky scroll transition, mobile hamburger, 5 nav links, CTA button renders. Footer with nav links, contact, mentions legales renders. Root layout wraps children with Header and Footer. lucide-react installed. Scroll reveal CSS classes available globally.</done>
</task>

<task type="auto">
  <name>Task 2: Create ScrollReveal and BeforeAfterSlider client components</name>
  <files>src/components/ui/ScrollReveal.tsx, src/components/ui/BeforeAfterSlider.tsx</files>
  <read_first>
    - src/app/globals.css (verify .reveal and .revealed classes exist from Task 1)
    - .planning/phases/02-pages-publiques/02-RESEARCH.md (Pattern 3 and Pattern 4 reference implementations)
    - .planning/phases/02-pages-publiques/02-UI-SPEC.md (Animations & Motion section, Resultats slider contract)
  </read_first>
  <action>
1. Create `src/components/ui/ScrollReveal.tsx` — Client Component (`'use client'`):
   - Props: `children: React.ReactNode`, `className?: string` (default `''`)
   - Uses `useRef<HTMLDivElement>` and `useEffect`
   - In useEffect: create `IntersectionObserver` with `{ threshold: 0.15 }`
   - On intersection: add class `revealed` to element, then `observer.disconnect()`
   - Cleanup: `observer.disconnect()` on unmount
   - Render: `<div ref={ref} className={`reveal ${className}`}>{children}</div>`

2. Create `src/components/ui/BeforeAfterSlider.tsx` — Client Component (`'use client'`):
   - Props interface:
     ```typescript
     interface BeforeAfterSliderProps {
       avantSrc: string
       apresSrc: string
       avantAlt: string
       apresAlt: string
     }
     ```
   - Uses `useRef<HTMLDivElement>` for container
   - Container: `relative aspect-[4/3] overflow-hidden rounded-t-2xl` with inline style `{ '--slider-pos': '50%' } as React.CSSProperties`
   - After image (background, full): `<Image>` from `next/image` with `fill`, `className="object-cover"`, `sizes="(max-width: 768px) 100vw, 50vw"`, `alt={apresAlt}`
   - Before image (clipped): wrapped in `<div className="absolute inset-0" style={{ clipPath: 'inset(0 calc(100% - var(--slider-pos)) 0 0)' }}>` containing `<Image>` with `fill`, `className="object-cover"`, `alt={avantAlt}`
   - Handle line: `<div className="absolute top-0 bottom-0 w-1 bg-bleu-electrique pointer-events-none" style={{ left: 'var(--slider-pos)', transform: 'translateX(-50%)' }} />`
   - Handle circle (on the line): `<div className="absolute top-1/2 w-10 h-10 bg-bleu-electrique rounded-full border-2 border-blanc-pur pointer-events-none flex items-center justify-center -translate-y-1/2" style={{ left: 'var(--slider-pos)', transform: 'translateX(-50%) translateY(-50%)' }}>` with `<ChevronLeft size={14} className="text-blanc-pur" /><ChevronRight size={14} className="text-blanc-pur" />` from `lucide-react`
   - Range input: `<input type="range" min="0" max="100" defaultValue="50" className="absolute inset-0 w-full h-full opacity-0 cursor-ew-resize" aria-label="Comparer avant et apres" />` with `onInput` handler that calls `containerRef.current?.style.setProperty('--slider-pos', `${e.target.value}%`)`
   - Fallback for no images: if `avantSrc` or `apresSrc` is empty string, render a `bg-gris-ardoise` placeholder div with text "Photo non disponible" centered
  </action>
  <verify>
    <automated>cd /home/lefeuc4/claude/SiteAntoine && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/ScrollReveal.tsx` contains `'use client'` on line 1
    - `src/components/ui/ScrollReveal.tsx` contains `IntersectionObserver`
    - `src/components/ui/ScrollReveal.tsx` contains `threshold: 0.15`
    - `src/components/ui/ScrollReveal.tsx` contains `revealed`
    - `src/components/ui/BeforeAfterSlider.tsx` contains `'use client'` on line 1
    - `src/components/ui/BeforeAfterSlider.tsx` contains `clip-path` or `clipPath`
    - `src/components/ui/BeforeAfterSlider.tsx` contains `--slider-pos`
    - `src/components/ui/BeforeAfterSlider.tsx` contains `type="range"`
    - `src/components/ui/BeforeAfterSlider.tsx` contains `aria-label`
    - `src/components/ui/BeforeAfterSlider.tsx` contains `next/image`
    - `pnpm build` completes without error
  </acceptance_criteria>
  <done>ScrollReveal wraps children and reveals on scroll via IntersectionObserver. BeforeAfterSlider renders two images with draggable clip-path slider, accessible via range input, uses next/image for optimization.</done>
</task>

<task type="auto">
  <name>Task 3: Create ServiceCard, ProgrammeCard, and ResultatCard components</name>
  <files>src/components/ui/ServiceCard.tsx, src/components/ui/ProgrammeCard.tsx, src/components/ui/ResultatCard.tsx</files>
  <read_first>
    - src/collections/Programmes.ts (field names and types for ProgrammeCard props)
    - src/collections/Resultats.ts (field names and types for ResultatCard props)
    - src/components/ui/BeforeAfterSlider.tsx (interface to import in ResultatCard)
    - .planning/phases/02-pages-publiques/02-UI-SPEC.md (card component contracts)
  </read_first>
  <action>
1. Create `src/components/ui/ServiceCard.tsx` — Server Component (no `'use client'`):
   - Props interface:
     ```typescript
     interface ServiceCardProps {
       titre: string
       description: string
       icone: string  // lucide-react icon name as string
     }
     ```
   - Import icons dynamically: create a simple icon map object at top of file mapping string names to lucide-react components: `{ 'dumbbell': Dumbbell, 'heart': Heart, 'users': Users, 'target': Target, 'trophy': Trophy, 'zap': Zap }` from `lucide-react`. Fallback to `Star` icon if name not found
   - Card: `bg-blanc-pur border border-gris-ardoise/20 rounded-xl p-6 hover:shadow-md hover:-translate-y-0.5 transition-all` with `style={{ transitionDuration: 'var(--transition-base)' }}`
   - Layout: icon (32px, `text-bleu-electrique mb-4`) + titre (`text-xl font-heading font-bold text-bleu-nuit mb-2`) + description (`text-base font-body text-gris-ardoise`)

2. Create `src/components/ui/ProgrammeCard.tsx` — Server Component (no `'use client'`):
   - Props interface:
     ```typescript
     interface ProgrammeCardProps {
       titre: string
       description: any  // Lexical rich text JSON — rendered via RichText component
       duree: string
       objectifs?: string | null
       publicCible?: string | null
       imageCouverture?: { url: string; alt?: string } | null
     }
     ```
   - Import `{ RichText } from '@payloadcms/richtext-lexical/react'` for description rendering
   - Card: `bg-blanc-pur border border-gris-ardoise/10 rounded-xl overflow-hidden hover:shadow-md hover:-translate-y-0.5 transition-all` with `style={{ transitionDuration: 'var(--transition-base)' }}`
   - Image area (top): if `imageCouverture` exists, render `<div className="relative aspect-video"><Image fill src={imageCouverture.url} alt={imageCouverture.alt || titre} className="object-cover" sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 33vw" /></div>`. If null, render placeholder: `<div className="aspect-video bg-gris-ardoise/20 flex items-center justify-center"><span className="text-gris-ardoise text-sm">Photo a venir</span></div>`
   - Body (`p-6`): titre (`text-xl font-heading font-bold text-bleu-nuit mb-2`), duration badge (`inline-block text-sm text-vert-energie bg-vert-energie/10 rounded-full px-3 py-1 mb-3`), description via `<RichText data={description} />` wrapped in `text-base font-body text-gris-ardoise mb-3`, objectifs as bullet list if present (`text-sm font-body text-bleu-nuit`), publicCible tag at bottom (`text-sm text-gris-ardoise`)

3. Create `src/components/ui/ResultatCard.tsx` — Server Component (no `'use client'`):
   - Props interface:
     ```typescript
     interface ResultatCardProps {
       prenomClient: string
       citation?: string | null
       programmeSuivi?: string | null
       duree?: string | null
       photoAvantUrl: string
       photoApresUrl: string
     }
     ```
   - Import `BeforeAfterSlider` from `@/components/ui/BeforeAfterSlider`
   - Card: `bg-blanc-pur rounded-2xl overflow-hidden shadow-sm border border-gris-ardoise/10`
   - Top: `<BeforeAfterSlider avantSrc={photoAvantUrl} apresSrc={photoApresUrl} avantAlt={`Photo avant de ${prenomClient}`} apresAlt={`Photo apres de ${prenomClient}`} />`
   - Citation section (`p-4`): if `citation` exists, render `<blockquote className="italic text-base font-body text-bleu-nuit mb-3"><span className="text-bleu-electrique text-2xl">"</span>{citation}<span className="text-bleu-electrique text-2xl">"</span></blockquote>`
   - Meta row: `<div className="flex flex-wrap gap-2 text-sm font-body text-gris-ardoise px-4 pb-4">` with prenom, programme suivi, duree separated by `·` separator
  </action>
  <verify>
    <automated>cd /home/lefeuc4/claude/SiteAntoine && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/ServiceCard.tsx` does NOT contain `'use client'`
    - `src/components/ui/ServiceCard.tsx` contains `lucide-react`
    - `src/components/ui/ServiceCard.tsx` contains `hover:shadow-md`
    - `src/components/ui/ProgrammeCard.tsx` does NOT contain `'use client'`
    - `src/components/ui/ProgrammeCard.tsx` contains `RichText` import from `@payloadcms/richtext-lexical/react`
    - `src/components/ui/ProgrammeCard.tsx` contains `text-vert-energie`
    - `src/components/ui/ProgrammeCard.tsx` contains `aspect-video`
    - `src/components/ui/ResultatCard.tsx` does NOT contain `'use client'`
    - `src/components/ui/ResultatCard.tsx` contains `import BeforeAfterSlider`
    - `src/components/ui/ResultatCard.tsx` contains `blockquote`
    - `pnpm build` completes without error
  </acceptance_criteria>
  <done>ServiceCard renders icon + title + description with hover effect. ProgrammeCard renders cover image, duration badge, rich text description, objectives, and target audience. ResultatCard renders BeforeAfterSlider, citation blockquote, and meta row. All three are Server Components (no 'use client').</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with zero errors
- Header.tsx, Footer.tsx exist in src/components/layout/
- ScrollReveal.tsx, BeforeAfterSlider.tsx, ServiceCard.tsx, ProgrammeCard.tsx, ResultatCard.tsx exist in src/components/ui/
- Root layout imports and renders Header and Footer
- globals.css contains .reveal and .revealed classes
- lucide-react is in package.json dependencies
</verification>

<success_criteria>
All shared layout and UI components are created, importable, and the Next.js build passes. The root layout renders Header and Footer on every page. Components are ready for page integration in Plans 03 and 04.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pages-publiques/02-01-SUMMARY.md`
</output>
