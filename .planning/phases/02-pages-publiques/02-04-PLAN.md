---
phase: 02-pages-publiques
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/app/services/page.tsx
  - src/app/programmes/page.tsx
  - src/app/resultats/page.tsx
autonomous: true
requirements:
  - PAGE-03
  - PAGE-04
  - PAGE-05

must_haves:
  truths:
    - "Mes Services page displays service cards in a responsive grid"
    - "Les Programmes page displays programme cards with duration badge, objectives, and audience"
    - "Resultats page displays result cards with before/after slider, citation, and metadata"
    - "All three pages consume data from Payload local API"
    - "Empty states display appropriate French messages when no data"
  artifacts:
    - path: "src/app/services/page.tsx"
      provides: "Mes Services page with card grid"
      contains: "getPayload"
    - path: "src/app/programmes/page.tsx"
      provides: "Les Programmes page with programme cards"
      contains: "getPayload"
    - path: "src/app/resultats/page.tsx"
      provides: "Resultats page with slider cards"
      contains: "getPayload"
  key_links:
    - from: "src/app/services/page.tsx"
      to: "payload local API"
      via: "getPayload for page-content mes-services section"
      pattern: "collection: 'page-content'"
    - from: "src/app/programmes/page.tsx"
      to: "payload local API"
      via: "getPayload for programmes collection"
      pattern: "collection: 'programmes'"
    - from: "src/app/resultats/page.tsx"
      to: "payload local API"
      via: "getPayload for resultats collection with depth 2"
      pattern: "collection: 'resultats'"
---

<objective>
Build the three remaining data-driven public pages: Mes Services, Les Programmes, and Resultats.

Purpose: These pages showcase Antoine's offerings (services, programmes) and social proof (before/after results with testimonials). They consume Payload collections directly and complete the 5-page public site.
Output: Three fully functional pages at `/services`, `/programmes`, and `/resultats`, all consuming Payload data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-pages-publiques/02-CONTEXT.md
@.planning/phases/02-pages-publiques/02-UI-SPEC.md
@.planning/phases/02-pages-publiques/02-01-SUMMARY.md

<interfaces>
<!-- Components available from Plan 01 -->
From src/components/ui/ServiceCard.tsx:
```typescript
interface ServiceCardProps { titre: string; description: string; icone: string }
```

From src/components/ui/ProgrammeCard.tsx:
```typescript
interface ProgrammeCardProps {
  titre: string; description: any; duree: string;
  objectifs?: string | null; publicCible?: string | null;
  imageCouverture?: { url: string; alt?: string } | null
}
```

From src/components/ui/ResultatCard.tsx:
```typescript
interface ResultatCardProps {
  prenomClient: string; citation?: string | null;
  programmeSuivi?: string | null; duree?: string | null;
  photoAvantUrl: string; photoApresUrl: string
}
```

From src/components/ui/ScrollReveal.tsx:
```typescript
export default function ScrollReveal({ children, className }: { children: React.ReactNode; className?: string })
```

<!-- Payload collections -->
PageContent slug: 'page-content', fields: page, section, titre, contenu (richText), image
Programmes slug: 'programmes', fields: titre, description (richText), duree, objectifs, publicCible, imageCouverture (upload->media), ordre
Resultats slug: 'resultats', fields: prenomClient, histoire (richText), programmeSuivi, duree, citation, photosAvant (array of {photo: upload->media}), photosApres (array of {photo: upload->media})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Mes Services and Les Programmes pages</name>
  <files>src/app/services/page.tsx, src/app/programmes/page.tsx</files>
  <read_first>
    - src/components/ui/ServiceCard.tsx (exact props interface)
    - src/components/ui/ProgrammeCard.tsx (exact props interface)
    - src/components/ui/ScrollReveal.tsx (wrapper usage)
    - src/collections/PageContent.ts (field names for query)
    - src/collections/Programmes.ts (field names for query)
    - .planning/phases/02-pages-publiques/02-UI-SPEC.md (Mes Services and Les Programmes contracts, Copywriting)
  </read_first>
  <action>
1. Create directory `src/app/services/` if not exists.

2. Create `src/app/services/page.tsx` — async Server Component:
   - Import: `getPayload` from `payload`, `config` from `@payload-config`, `ServiceCard`, `ScrollReveal`, `Link` from `next/link`
   - Export metadata: `{ title: "Services de Coaching — Antoine Profit" }`
   - Fetch services: `payload.find({ collection: 'page-content', where: { page: { equals: 'services' }, section: { equals: 'mes-services' } } })`
   - Extract plain text from richText `contenu` field: `doc.contenu?.root?.children?.[0]?.children?.[0]?.text || ''`
   - Icon map by index: index 0 → 'heart', index 1 → 'users', index 2 → 'zap' (matching seed data order: Consultation Individuelle, Coaching en Groupe, Programme a Distance)
   - Main container: `<main className="max-w-[1280px] mx-auto px-8 lg:px-12 py-12 lg:py-16">`
   - H1: `"Mes Services"` — `text-4xl font-heading font-bold text-bleu-nuit mb-12 text-center`
   - Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
   - Each service: `<ScrollReveal key={doc.id}><ServiceCard titre={doc.titre || ''} description={extractedText} icone={iconMap[i] || 'star'} /></ScrollReveal>`
   - After grid, CTA section: `<div className="text-center mt-12"><Link href="/contact" className="bg-bleu-electrique text-blanc-pur rounded-full px-6 py-3 font-heading text-sm inline-block hover:brightness-110 transition" style={{ transitionDuration: 'var(--transition-fast)' }}>Prendre contact</Link></div>`
   - Empty state: if no docs, render `<p className="text-gris-ardoise text-center text-base">Les services seront bientot disponibles.</p>`

3. Create directory `src/app/programmes/` if not exists.

4. Create `src/app/programmes/page.tsx` — async Server Component:
   - Import: `getPayload` from `payload`, `config` from `@payload-config`, `ProgrammeCard`, `ScrollReveal`
   - Export metadata: `{ title: "Programmes de Coaching — Antoine Profit" }`
   - Fetch programmes: `payload.find({ collection: 'programmes', sort: 'ordre', limit: 20, depth: 2 })`
   - Main container: `<main className="max-w-[1280px] mx-auto px-8 lg:px-12 py-12 lg:py-16">`
   - H1: `"Les Programmes"` — `text-4xl font-heading font-bold text-bleu-nuit mb-12 text-center`
   - Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
   - Each programme: `<ScrollReveal key={prog.id}><ProgrammeCard titre={prog.titre} description={prog.description} duree={prog.duree} objectifs={prog.objectifs} publicCible={prog.publicCible} imageCouverture={prog.imageCouverture && typeof prog.imageCouverture === 'object' && 'url' in prog.imageCouverture ? { url: prog.imageCouverture.url as string, alt: (prog.imageCouverture as any).alt } : null} /></ScrollReveal>`
   - Empty state: `<p className="text-gris-ardoise text-center text-base">Aucun programme disponible pour le moment. Revenez bientot.</p>`
  </action>
  <verify>
    <automated>cd /home/lefeuc4/claude/SiteAntoine && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/services/page.tsx` contains `getPayload`
    - `src/app/services/page.tsx` contains `collection: 'page-content'`
    - `src/app/services/page.tsx` contains `section: { equals: 'mes-services' }`
    - `src/app/services/page.tsx` contains `Mes Services` as H1
    - `src/app/services/page.tsx` contains `Services de Coaching — Antoine Profit` in metadata
    - `src/app/services/page.tsx` contains `ServiceCard`
    - `src/app/services/page.tsx` contains `Prendre contact`
    - `src/app/programmes/page.tsx` contains `getPayload`
    - `src/app/programmes/page.tsx` contains `collection: 'programmes'`
    - `src/app/programmes/page.tsx` contains `sort: 'ordre'`
    - `src/app/programmes/page.tsx` contains `Les Programmes` as H1
    - `src/app/programmes/page.tsx` contains `Programmes de Coaching — Antoine Profit` in metadata
    - `src/app/programmes/page.tsx` contains `ProgrammeCard`
    - `src/app/programmes/page.tsx` contains `Aucun programme disponible` (empty state)
    - `pnpm build` completes without error
  </acceptance_criteria>
  <done>Mes Services page renders service cards in 3-column responsive grid from PageContent data. Les Programmes page renders programme cards with duration badges from Programmes collection sorted by ordre. Both have empty states and CTA links.</done>
</task>

<task type="auto">
  <name>Task 2: Create Resultats page with before/after slider cards</name>
  <files>src/app/resultats/page.tsx</files>
  <read_first>
    - src/components/ui/ResultatCard.tsx (exact props interface — photoAvantUrl, photoApresUrl)
    - src/components/ui/BeforeAfterSlider.tsx (verify it handles empty string src gracefully)
    - src/components/ui/ScrollReveal.tsx (wrapper component)
    - src/collections/Resultats.ts (exact field structure — photosAvant array of {photo: upload->media})
    - .planning/phases/02-pages-publiques/02-UI-SPEC.md (Resultats page contract, Copywriting)
  </read_first>
  <action>
1. Create directory `src/app/resultats/` if not exists.

2. Create `src/app/resultats/page.tsx` — async Server Component:
   - Import: `getPayload` from `payload`, `config` from `@payload-config`, `ResultatCard`, `ScrollReveal`
   - Export metadata: `{ title: "Resultats — Antoine Profit" }`
   - Fetch resultats: `payload.find({ collection: 'resultats', limit: 20, depth: 2 })` — depth 2 populates the Media relation inside photosAvant/photosApres arrays
   - Main container: `<main className="max-w-[1280px] mx-auto px-8 lg:px-12 py-12 lg:py-16">`
   - H1: `"Resultats & Temoignages"` — `text-4xl font-heading font-bold text-bleu-nuit mb-4 text-center`
   - Subtitle: `<p className="text-base font-body text-gris-ardoise text-center mb-12 max-w-2xl mx-auto">Decouvrez les transformations de mes clients. Chaque parcours est unique, chaque resultat est le fruit d'un engagement mutuel.</p>`
   - Grid: `grid grid-cols-1 md:grid-cols-2 gap-6`
   - For each resultat, extract photo URLs:
     ```typescript
     const photoAvantUrl = (() => {
       const photo = resultat.photosAvant?.[0]?.photo
       if (photo && typeof photo === 'object' && 'url' in photo) return photo.url as string
       return ''
     })()
     const photoApresUrl = (() => {
       const photo = resultat.photosApres?.[0]?.photo
       if (photo && typeof photo === 'object' && 'url' in photo) return photo.url as string
       return ''
     })()
     ```
   - Render: `<ScrollReveal key={resultat.id}><ResultatCard prenomClient={resultat.prenomClient} citation={resultat.citation} programmeSuivi={resultat.programmeSuivi} duree={resultat.duree} photoAvantUrl={photoAvantUrl} photoApresUrl={photoApresUrl} /></ScrollReveal>`
   - Empty state: `<p className="text-gris-ardoise text-center text-base">Les premiers resultats arrivent bientot.</p>`
  </action>
  <verify>
    <automated>cd /home/lefeuc4/claude/SiteAntoine && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/resultats/page.tsx` contains `getPayload`
    - `src/app/resultats/page.tsx` contains `collection: 'resultats'`
    - `src/app/resultats/page.tsx` contains `depth: 2`
    - `src/app/resultats/page.tsx` contains `Resultats & Temoignages` as H1
    - `src/app/resultats/page.tsx` contains `Resultats — Antoine Profit` in metadata
    - `src/app/resultats/page.tsx` contains `ResultatCard`
    - `src/app/resultats/page.tsx` contains `photosAvant`
    - `src/app/resultats/page.tsx` contains `photosApres`
    - `src/app/resultats/page.tsx` contains `Les premiers resultats arrivent bientot` (empty state)
    - `src/app/resultats/page.tsx` contains `ScrollReveal`
    - `pnpm build` completes without error
  </acceptance_criteria>
  <done>Resultats page renders result cards in 2-column responsive grid. Each card shows before/after slider, client citation, and metadata (prenom, programme, duration). Photos extracted from Payload with depth 2 to populate Media relations. Empty state message when no results exist.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with zero errors
- `/services` page renders service cards from PageContent
- `/programmes` page renders programme cards from Programmes collection
- `/resultats` page renders result cards with slider from Resultats collection
- All three pages have proper metadata titles
- All three pages have empty state messages
</verification>

<success_criteria>
Three data-driven pages are complete. Mes Services displays service cards. Les Programmes displays programme cards with duration badges. Resultats displays before/after slider cards with citations. All pages consume Payload data and the Next.js build passes. Combined with Plans 01-03, all 5 public pages are now built.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pages-publiques/02-04-SUMMARY.md`
</output>
