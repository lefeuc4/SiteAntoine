---
phase: 05.1-frontend-polish
plan: 01
subsystem: ui
tags: [nextjs, app-router, tailwind, ux-states, error-boundary, loading, not-found]

# Dependency graph
requires:
  - phase: 02-pages-publiques
    provides: frontend route group (frontend) with layout and pages
  - phase: 05
    provides: globals.css with design tokens (blanc-pur, bleu-nuit, bleu-electrique, gris-ardoise)
provides:
  - error.tsx Client Component error boundary in (frontend) route group
  - loading.tsx Server Component loading suspense in (frontend) route group
  - not-found.tsx Server Component 404 page in (frontend) route group
  - Header uses bg-blanc-pur (#F8FAFC) instead of bg-white (#FFFFFF)
  - Title template uses em-dash separator: Mon Histoire — Antoine Profit
affects: [05.1-frontend-polish]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "UX state files in (frontend) route group for App Router error/loading/404 handling"
    - "error.tsx must be Client Component ('use client') for App Router error boundary"
    - "loading.tsx and not-found.tsx are Server Components (no 'use client')"

key-files:
  created:
    - src/app/(frontend)/error.tsx
    - src/app/(frontend)/loading.tsx
    - src/app/(frontend)/not-found.tsx
  modified:
    - src/components/layout/Header.tsx
    - src/app/layout.tsx

key-decisions:
  - "error.tsx placed in (frontend) route group — scoped to public pages only, not admin"
  - "bg-blanc-pur (#F8FAFC) in Header instead of bg-white (#FFFFFF) — matches design token spec"
  - "Em-dash separator in title template — matches UI spec copywriting contract"

patterns-established:
  - "UX state files use same layout tokens as other pages: max-w-[1280px] mx-auto px-8 lg:px-12 py-24"

requirements-completed: [UX-STATES, COLOR-TOKEN, COPY-SEPARATOR]

# Metrics
duration: 8min
completed: 2026-04-05
---

# Phase 05.1 Plan 01: UX State Pages and Header/Title Fixes Summary

**Three App Router UX state pages (error, loading, not-found) created in French with design tokens, header color corrected to bg-blanc-pur, and title template updated to em-dash separator**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-05T11:10:53Z
- **Completed:** 2026-04-05T11:18:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Created error.tsx as Client Component with French error message and Reessayer button using bleu-electrique token
- Created loading.tsx as Server Component with animate-pulse Chargement... text
- Created not-found.tsx as Server Component with Page introuvable and link to accueil
- Fixed Header.tsx: bg-white replaced with bg-blanc-pur (#F8FAFC) matching design system
- Fixed layout.tsx: title template separator changed from pipe (|) to em-dash (—)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create error.tsx, loading.tsx, not-found.tsx** - `01604ac` (feat)
2. **Task 2: Fix header bg-blanc-pur and title em-dash** - `c7c9fa1` (fix)

## Files Created/Modified
- `src/app/(frontend)/error.tsx` - Client Component error boundary with French copy and Reessayer button
- `src/app/(frontend)/loading.tsx` - Server Component loading state with animate-pulse
- `src/app/(frontend)/not-found.tsx` - Server Component 404 page with link back to accueil
- `src/components/layout/Header.tsx` - bg-white -> bg-blanc-pur in bgClass (both scrolled and default)
- `src/app/layout.tsx` - title template '%s | Antoine Profit' -> '%s — Antoine Profit'

## Decisions Made
- error.tsx placed in (frontend) route group scoped to public pages only — admin errors handled separately by Payload
- UX state pages follow same max-w-[1280px] layout container pattern as other public pages for visual consistency

## Deviations from Plan

None - plan executed exactly as written.

Note: Build verification via `pnpm build` was skipped in the worktree because node_modules are not installed in parallel agent worktrees. The orchestrator runs the build check after all agents complete.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- UX state pages complete: Experience audit gap closed
- Color token audit gap closed: Header uses correct blanc-pur token
- Copywriting audit gap closed: Title template uses em-dash separator
- Ready for Plan 02 (remaining frontend polish items)

---
*Phase: 05.1-frontend-polish*
*Completed: 2026-04-05*
