---
phase: 05.1
slug: frontend-polish
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-05
---

# Phase 05.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — no test runner configured in this project |
| **Automated checks** | grep-based static assertions + `pnpm build` compilation check |
| **Quick run command** | `pnpm build` |
| **Full suite command** | `pnpm build` (no unit/e2e tests exist) |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run the task's `<automated>` verify command
- **After every plan wave:** Run `pnpm build` to confirm no compilation errors
- **Before `/gsd:verify-work`:** All grep checks must return expected results + `pnpm build` succeeds
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | Status |
|---------|------|------|-------------|-----------|-------------------|--------|
| 01-T1 | 01 | 1 | UX-STATES | grep + ls | `ls src/app/\(frontend\)/error.tsx src/app/\(frontend\)/loading.tsx src/app/\(frontend\)/not-found.tsx && grep -q "'use client'" src/app/\(frontend\)/error.tsx && grep -q "Chargement" src/app/\(frontend\)/loading.tsx && grep -q "Page introuvable" src/app/\(frontend\)/not-found.tsx` | pending |
| 01-T2 | 01 | 1 | COLOR-TOKEN, COPY-SEPARATOR | grep | `grep "bg-blanc-pur" src/components/layout/Header.tsx \| grep -v "bg-white" && grep "template:" src/app/layout.tsx \| grep "---"` | pending |
| 02-T1 | 02 | 1 | TYPO-TOKENS, SPACING-TOKENS | grep (zero-match) | `grep -r "text-2xl" src/app/\(frontend\)/ src/components/ ; grep -r "text-\[" src/app/\(frontend\)/ src/components/ ; grep -r "left-\[" src/ ; echo "EXIT: $?"` | pending |
| 02-T2 | 02 | 1 | TIMELINE-YEAR, FOCUS-RINGS | grep | `grep "parseTitre" src/app/\(frontend\)/mon-histoire/page.tsx && grep "focus-visible:ring-2" src/components/ui/ServiceCard.tsx src/components/ui/ProgrammeCard.tsx src/components/ui/ResultatCard.tsx && grep "tabIndex" src/components/ui/ServiceCard.tsx src/components/ui/ProgrammeCard.tsx src/components/ui/ResultatCard.tsx` | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

No Wave 0 needed. No test infrastructure to set up. All automated verification uses grep-based static assertions against source files and `pnpm build` for compilation. Existing infrastructure covers all phase requirements.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| error.tsx renders on fetch failure | UX-STATES | Requires DB disconnection or thrown error | Stop DB connection, reload any frontend page, verify French error copy appears with "Reessayer" button |
| loading.tsx shown on slow page load | UX-STATES | Requires artificial delay | Add `await new Promise(r => setTimeout(r, 3000))` temporarily in any page fetch, verify "Chargement..." pulse text |
| not-found.tsx shown on invalid URL | UX-STATES | Requires browser navigation | Navigate to `/page-inexistante`, verify branded 404 with "Retour a l'accueil" link |
| Header background color correct | COLOR-TOKEN | Visual check (computed style) | DevTools > Computed styles on `<header>` must show #F8FAFC |
| Title separator in browser tab | COPY-SEPARATOR | Requires browser tab inspection | Navigate to any inner page, check tab shows "Page --- Antoine Profit" |
| Timeline year labels visible | TIMELINE-YEAR | Visual check | Navigate to `/mon-histoire`, verify "2010", "2013", etc. appear above each entry title |
| Card focus rings visible on keyboard tab | FOCUS-RINGS | Requires keyboard interaction | Tab through `/services` page, verify blue ring visible on each card |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify commands (grep-based)
- [x] Sampling continuity: all 4 tasks have automated verify
- [x] Wave 0 covers all MISSING references (none needed)
- [x] No watch-mode flags
- [x] Feedback latency < 30s (grep is instant, pnpm build ~30s)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
