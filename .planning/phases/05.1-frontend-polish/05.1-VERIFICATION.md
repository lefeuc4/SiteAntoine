---
phase: 05.1-frontend-polish
verified: 2026-04-05T12:00:00Z
status: passed
score: 9/9 must-haves verified
re_verification: false
human_verification:
  - test: "Navigate to a non-existent URL (e.g. /page-inexistante) in the deployed app"
    expected: "Branded 404 page in French appears with 'Page introuvable' heading and a 'Retour a l accueil' link"
    why_human: "Next.js not-found.tsx activation requires a running server with matching App Router route resolution"
  - test: "Tab through cards on /services, /programmes, and /resultats using keyboard only"
    expected: "Each card receives a visible blue focus ring (bleu-electrique) when focused"
    why_human: "focus-visible pseudo-class behavior requires a running browser environment"
  - test: "Visit /mon-histoire and verify year labels appear above entry titles"
    expected: "Each timeline entry shows a grey year label (e.g. '2010') above the entry title"
    why_human: "parseTitre() depends on Payload CMS seed data having 'YYYY — Title' format; requires running DB"
---

# Phase 05.1: Frontend Polish Verification Report

**Phase Goal:** Corriger les 12 deviations identifiees par l'audit UI (score 17/24) — UX states manquants, tokens typographiques/couleur/espacement non conformes, labels annees timeline, focus rings accessibilite
**Verified:** 2026-04-05T12:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                    | Status     | Evidence                                                                                         |
|----|----------------------------------------------------------------------------------------------------------|------------|--------------------------------------------------------------------------------------------------|
| 1  | Navigating to /page-inexistante shows a branded 404 page in French with a link to accueil               | VERIFIED   | not-found.tsx exists, contains "Page introuvable" and `href="/"`                                 |
| 2  | When a server error occurs, the user sees a French error message with a Reessayer button                 | VERIFIED   | error.tsx first line is `'use client'`, contains "Oups, une erreur est survenue", `onClick={reset}` |
| 3  | While a page loads (slow DB), the user sees Chargement... pulse text                                     | VERIFIED   | loading.tsx contains `animate-pulse` and "Chargement..."                                         |
| 4  | The header background uses blanc-pur (#F8FAFC) not white (#FFFFFF)                                       | VERIFIED   | Header.tsx line 29: `bg-blanc-pur shadow-sm` / `bg-blanc-pur` — zero occurrences of `bg-white`   |
| 5  | Browser tab titles on inner pages use em-dash separator (Mon Histoire — Antoine Profit)                  | VERIFIED   | layout.tsx template: `'%s — Antoine Profit'` (em-dash U+2014 confirmed, pipe absent)            |
| 6  | Zero occurrences of text-2xl in src/app/(frontend)/ and src/components/                                  | VERIFIED   | grep returns 0 results across all tsx/ts files                                                   |
| 7  | Zero occurrences of text-[ arbitrary font values in src/app/(frontend)/ and src/components/              | VERIFIED   | grep returns 0 results in both search paths                                                       |
| 8  | Zero occurrences of left-[ arbitrary spacing in src/                                                     | VERIFIED   | grep returns 0 results across entire src/                                                        |
| 9  | Timeline entries on /mon-histoire show year labels (2010, 2013, etc.) above each entry title             | VERIFIED   | parseTitre() defined at line 21, called at line 70; year rendered at line 86 as `<span className="text-sm font-body text-gris-ardoise block mb-1">` |

**Score:** 9/9 truths verified

---

### Required Artifacts

| Artifact                                           | Provides                            | Status     | Details                                                                |
|----------------------------------------------------|-------------------------------------|------------|------------------------------------------------------------------------|
| `src/app/(frontend)/error.tsx`                     | Error boundary page                 | VERIFIED   | Exists, 27 lines, `'use client'` on line 1, contains `bg-bleu-electrique`, `onClick={reset}` |
| `src/app/(frontend)/loading.tsx`                   | Loading suspense page               | VERIFIED   | Exists, 9 lines, no 'use client', contains `animate-pulse` and "Chargement..." |
| `src/app/(frontend)/not-found.tsx`                 | 404 page                            | VERIFIED   | Exists, 22 lines, contains "Page introuvable" and `href="/"` Link     |
| `src/app/(frontend)/mon-histoire/page.tsx`         | Timeline with year label parsing    | VERIFIED   | parseTitre function at line 21; annee rendered conditionally at line 85-87 |
| `src/components/ui/ResultatCard.tsx`               | Quote decorators at text-xl         | VERIFIED   | Lines 51/53 use `text-xl not-italic`; tabIndex={0} on wrapper div     |
| `src/components/ui/ServiceCard.tsx`                | Card with focus ring and tabIndex   | VERIFIED   | tabIndex={0} at line 34, `focus-visible:ring-2 focus-visible:ring-bleu-electrique focus-visible:outline-none` |

---

### Key Link Verification

| From                                      | To                                 | Via                                     | Status   | Details                                                       |
|-------------------------------------------|------------------------------------|-----------------------------------------|----------|---------------------------------------------------------------|
| `error.tsx`                               | Next.js App Router error boundary  | `'use client'` directive + reset prop   | WIRED    | Line 1: `'use client'`; `reset: () => void` in props; `onClick={reset}` used |
| `not-found.tsx`                           | `/`                                | Link component with href="/"            | WIRED    | `import Link from 'next/link'`; `href="/"` present on line 14 |
| `mon-histoire/page.tsx`                   | PageContent.titre field            | parseTitre() string parsing             | WIRED    | parseTitre called with `entry.titre || ''`; destructured result used in JSX |
| `Header.tsx` bgClass                      | Design token blanc-pur             | Tailwind class `bg-blanc-pur`           | WIRED    | Line 29: both scrolled and default state use `bg-blanc-pur`   |
| `layout.tsx` title template               | Em-dash separator contract         | Unicode U+2014 in template string       | WIRED    | `'%s — Antoine Profit'` confirmed by char inspection; pipe absent |

---

### Data-Flow Trace (Level 4)

| Artifact                              | Data Variable          | Source                                        | Produces Real Data | Status    |
|---------------------------------------|------------------------|-----------------------------------------------|--------------------|-----------|
| `mon-histoire/page.tsx`               | `entries` (timeline)   | `payload.find({ collection: 'page-content' })` | Yes — DB query     | FLOWING   |
| `error.tsx`                           | `reset` prop           | Next.js App Router (framework-injected)        | Yes — framework    | FLOWING   |
| `loading.tsx`                         | Static UI only         | N/A (no dynamic data)                         | N/A                | N/A       |
| `not-found.tsx`                       | Static UI only         | N/A (no dynamic data)                         | N/A                | N/A       |

---

### Behavioral Spot-Checks

Step 7b: SKIPPED — app requires running Next.js server + Neon PostgreSQL connection; no standalone runnable entry points available in this environment. Key behaviors routed to human verification above.

---

### Requirements Coverage

The requirement IDs in PLAN frontmatter (UX-STATES, COLOR-TOKEN, COPY-SEPARATOR, TYPO-TOKENS, SPACING-TOKENS, TIMELINE-YEAR, FOCUS-RINGS) are **phase-local audit identifiers** defined in ROADMAP.md for Phase 05.1. They do not correspond to entries in `.planning/REQUIREMENTS.md`, which uses the PAGE-xx, DSGN-xx, ADMN-xx, etc. naming scheme.

**Finding:** These IDs represent UI-audit deviations identified in the Phase 05.1 research (05.1-RESEARCH.md / 05.1-UI-SPEC.md), not v1 feature requirements. They are not orphaned requirements from REQUIREMENTS.md — they are a separate audit-driven ID space. No v1 REQUIREMENTS.md entry maps to Phase 05.1, which is consistent with Phase 05.1 being an inserted polishing phase rather than a feature phase.

| Requirement ID  | Source      | Description                                        | Status     | Evidence                                              |
|-----------------|-------------|----------------------------------------------------|------------|-------------------------------------------------------|
| UX-STATES       | ROADMAP 05.1 | UX state files (error, loading, not-found) missing | SATISFIED  | All 3 files created with correct content              |
| COLOR-TOKEN     | ROADMAP 05.1 | Header bg-white instead of bg-blanc-pur            | SATISFIED  | Header.tsx uses bg-blanc-pur, zero bg-white           |
| COPY-SEPARATOR  | ROADMAP 05.1 | Title template uses pipe instead of em-dash        | SATISFIED  | layout.tsx template uses em-dash U+2014               |
| TYPO-TOKENS     | ROADMAP 05.1 | text-2xl and text-[...] arbitrary values present   | SATISFIED  | Zero text-2xl, zero text-[ in src/                   |
| SPACING-TOKENS  | ROADMAP 05.1 | left-[-2rem] arbitrary spacing in timeline         | SATISFIED  | Zero left-[ in src/; replaced with -left-8           |
| TIMELINE-YEAR   | ROADMAP 05.1 | Year labels missing from timeline entries          | SATISFIED  | parseTitre() extracts and renders year above each h3 |
| FOCUS-RINGS     | ROADMAP 05.1 | Cards not keyboard-focusable, no focus rings       | SATISFIED  | All 3 cards: tabIndex={0} + focus-visible:ring-2     |

**Note on REQUIREMENTS.md traceability:** Phase 05.1 requirement IDs are not registered in `.planning/REQUIREMENTS.md` Traceability table. This is a documentation gap — these polish requirements exist only in ROADMAP.md. No action required for goal achievement; flagged for awareness.

---

### Anti-Patterns Found

| File                                              | Line | Pattern                              | Severity | Impact               |
|---------------------------------------------------|------|--------------------------------------|----------|----------------------|
| `src/app/(frontend)/mentions-legales/page.tsx`   | 44   | `text-xl font-bold` (missing `font-heading`) on fallback h2 headings | Info | Fallback content only (shown when Payload has no rich text content); h2s in static fallback block inconsistently styled vs the h1 which has `font-heading`. Not a regression from this phase — these h2s were pre-existing. |

No blockers. No stub patterns in phase-modified files. No TODO/FIXME. No hardcoded empty state that flows to user-visible output.

---

### Human Verification Required

#### 1. 404 Page Activation

**Test:** Navigate to a non-existent URL (e.g. /page-inexistante) in the deployed or locally running app
**Expected:** Branded 404 page appears with "Page introuvable" heading, grey subtitle, and blue "Retour a l'accueil" button linking to /
**Why human:** Next.js not-found.tsx only activates through App Router's notFound() signal or unmatched routes — requires running server

#### 2. Keyboard Focus Rings on Cards

**Test:** On /services, /programmes, and /resultats pages, press Tab to cycle through cards using keyboard only
**Expected:** Each card (service, programme, resultat) displays a visible blue focus ring (2px solid bleu-electrique) when focused; no outline flash from native browser default
**Why human:** focus-visible pseudo-class behavior requires a live browser with keyboard navigation; CSS utility classes cannot be verified to produce visible rings from static analysis alone

#### 3. Timeline Year Labels with Real Seed Data

**Test:** With the database seeded (pnpm seed), visit /mon-histoire
**Expected:** Each timeline entry displays a small grey year label (e.g. "2010") above the bold entry title; titles show only the name part (e.g. "Les Premiers Pas") without the year prefix
**Why human:** parseTitre() parsing result depends on Payload seed data format matching `"YYYY — Title"` pattern; requires running DB

---

### Gaps Summary

No gaps. All 9 observable truths verified. All artifacts exist, are substantive, and are wired. All 7 phase requirement IDs satisfied. No blocker anti-patterns found.

The phase goal — correcting the 12 UI audit deviations (score 17/24) — is achieved at the source level. Three behavioral checks are routed to human verification as they require a running server/browser environment.

---

_Verified: 2026-04-05T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
