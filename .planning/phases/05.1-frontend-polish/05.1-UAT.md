---
status: complete
phase: 05.1-frontend-polish
source: [05.1-01-SUMMARY.md, 05.1-02-SUMMARY.md]
started: 2026-04-05T14:00:00Z
updated: 2026-04-05T14:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Error Page (French)
expected: Trigger a runtime error on a public page. You should see a French error message with a "Reessayer" button styled in bleu-electrique, inside the standard layout container.
result: skipped
reason: Requires artificially triggering a runtime error — not practical to test manually

### 2. Loading State
expected: During page navigation or slow load, a "Chargement..." text with pulse animation appears as a loading indicator.
result: pass

### 3. 404 Not Found Page
expected: Navigate to a non-existent route (e.g. /une-page-qui-nexiste-pas). You should see "Page introuvable" in French with a link back to the homepage (accueil).
result: issue
reported: "no j'ai 404 This page could not be found."
severity: major

### 4. Header Background Color
expected: The site header uses the blanc-pur color (#F8FAFC) as background, not pure white (#FFFFFF). It should be a very subtle off-white, consistent whether scrolled or at top.
result: pass

### 5. Title Template Separator
expected: Browser tab titles on subpages use an em-dash separator: e.g. "Mon Histoire — Antoine Profit" (not a pipe character).
result: pass

### 6. Typography Token Consistency
expected: Page text across contact, mentions-legales, and mon-histoire pages uses standard Tailwind sizes (no visually broken or oversized text). The mentions-legales heading uses the Montserrat heading font.
result: pass

### 7. Timeline Year Labels (Mon Histoire)
expected: On the Mon Histoire page, timeline entries that have a year in the title (e.g. "2020 — Formation") display the year as a small label above the entry title.
result: pass

### 8. Card Focus Rings (Keyboard Navigation)
expected: Tab through ServiceCard, ProgrammeCard, and ResultatCard elements using keyboard. Each card shows a visible bleu-electrique focus ring when focused via keyboard (focus-visible).
result: pass

## Summary

total: 8
passed: 6
issues: 1
pending: 0
skipped: 1
blocked: 0

## Gaps

- truth: "Navigate to non-existent route shows French 'Page introuvable' with link to accueil"
  status: failed
  reason: "User reported: default Next.js 404 'This page could not be found' shown instead of custom French page"
  severity: major
  test: 3
  root_cause: "not-found.tsx is in src/app/(frontend)/ route group — only catches notFound() calls within that group. Next.js requires a root-level src/app/not-found.tsx to catch top-level 404s."
  artifacts:
    - path: "src/app/(frontend)/not-found.tsx"
      issue: "Scoped to route group, not root-level"
  missing:
    - "Add src/app/not-found.tsx at root level (or move existing file)"
  debug_session: ""
