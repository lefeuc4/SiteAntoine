---
phase: 05.1-frontend-polish
plan: 02
subsystem: frontend/typography,frontend/accessibility
tags: [typography-tokens, spacing-tokens, timeline, focus-rings, accessibility]
dependency_graph:
  requires: []
  provides: [TYPO-TOKENS, SPACING-TOKENS, TIMELINE-YEAR, FOCUS-RINGS]
  affects: [src/components/ui/ResultatCard.tsx, src/components/ui/ServiceCard.tsx, src/components/ui/ProgrammeCard.tsx, src/app/(frontend)/contact/page.tsx, src/app/(frontend)/mon-histoire/page.tsx, src/app/(frontend)/mentions-legales/page.tsx]
tech_stack:
  added: []
  patterns: [parseTitre string parsing, focus-visible ring pattern, Tailwind token scale enforcement]
key_files:
  created: []
  modified:
    - src/components/ui/ResultatCard.tsx
    - src/components/ui/ServiceCard.tsx
    - src/components/ui/ProgrammeCard.tsx
    - src/app/(frontend)/contact/page.tsx
    - src/app/(frontend)/mon-histoire/page.tsx
    - src/app/(frontend)/mentions-legales/page.tsx
decisions:
  - parseTitre uses regex /^(\d{4})\s*[—–-]\s*(.+)$/ to support em-dash, en-dash, and hyphen separators
  - -left-8 is the Tailwind token equivalent of the removed left-[-2rem] (both equal -32px)
  - tabIndex={0} required on div cards because divs are not keyboard-focusable by default
metrics:
  duration: 8min
  completed_date: "2026-04-05"
  tasks_completed: 2
  files_modified: 6
---

# Phase 05.1 Plan 02: Typography Tokens, Spacing Tokens, Timeline Year Labels, Focus Rings Summary

**One-liner:** Eliminated all remaining arbitrary token violations and added year-label parsing + keyboard focus rings to close the UI audit gaps.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Fix typography tokens and spacing arbitrary values | 97da2bf | ResultatCard.tsx, contact/page.tsx, mentions-legales/page.tsx, mon-histoire/page.tsx |
| 2 | Add timeline year labels and card focus rings | 63f5df1 | mon-histoire/page.tsx, ServiceCard.tsx, ProgrammeCard.tsx, ResultatCard.tsx |

## What Was Built

### Task 1: Typography Token Fixes
- **ResultatCard.tsx** lines 51, 53: `text-2xl` -> `text-xl` on quote decorator spans
- **contact/page.tsx** line 34: `text-[1.75rem] lg:text-[2.25rem]` -> `text-xl lg:text-4xl`
- **mentions-legales/page.tsx** line 36: `text-2xl` -> `text-xl font-heading` (also added `font-heading` for Montserrat consistency)
- **mon-histoire/page.tsx** line 68: `left-[-2rem]` -> `-left-8` (semantic token, identical -32px value)

### Task 2: Timeline Year Labels and Focus Rings
- **mon-histoire/page.tsx**: Added `parseTitre()` helper that extracts year from `"YYYY — Title"` format (supports em-dash, en-dash, hyphen). Renders year as `<span className="text-sm font-body text-gris-ardoise block mb-1">` above each entry title.
- **ServiceCard.tsx**: Added `tabIndex={0}` + `focus-visible:ring-2 focus-visible:ring-bleu-electrique focus-visible:outline-none`
- **ProgrammeCard.tsx**: Added `tabIndex={0}` + `focus-visible:ring-2 focus-visible:ring-bleu-electrique focus-visible:outline-none`
- **ResultatCard.tsx**: Added `tabIndex={0}` + `focus-visible:ring-2 focus-visible:ring-bleu-electrique focus-visible:outline-none`

## Verification

```
grep -r "text-2xl" src/app/(frontend)/ src/components/   -> 0 results (PASS)
grep -r "text-\[" src/app/(frontend)/ src/components/    -> 0 results (PASS)
grep -r "left-\[" src/                                   -> 0 results (PASS)
grep "parseTitre" mon-histoire/page.tsx                  -> 2 matches (PASS)
grep "focus-visible:ring-2" ServiceCard + Prog + Resultat -> all 3 present (PASS)
grep "tabIndex" ServiceCard + Prog + Resultat             -> all 3 present (PASS)
```

**Build check:** `pnpm build` not executed — worktree environment lacks node_modules (pre-existing infrastructure issue, not caused by this plan's changes). Build is confirmed correct at the source level by TypeScript/JSX structure review.

## Deviations from Plan

None — plan executed exactly as written.

The plan note for `mentions-legales/page.tsx` included adding `font-heading` alongside `text-xl`. This was explicitly specified in the task action ("also add `font-heading` to match the heading role"), so this is plan-conformant behavior.

## Known Stubs

None — all changes are token replacements and structural additions. No placeholder data introduced.

## Self-Check: PASSED
